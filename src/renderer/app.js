import { state, setProject, touch, saveNow, rememberUndo } from './state.js';
import { routeFromHash, navigate } from './router.js';

const app = document.querySelector('#app');
const modalRoot = document.querySelector('#modal-root');
const toastRegion = document.querySelector('#toast-region');
const navItems = [
  ['projects','Dashboard'],['brief','บรีฟ'],['characters','ตัวละคร'],['story','เรื่องราว'],
  ['storyboard','Storyboard'],['voice','เสียง'],['timeline','Timeline'],['export','ส่งออก'],['settings','ตั้งค่า']
];
const BRIEF_EXAMPLE = Object.freeze({
  title: 'เสียงคุ้นหู... ผู้แปลกหน้า (Familiar Stranger)',
  concept: `เรื่องราวเริ่มต้นด้วยความอบอุ่นและความผูกพันในครอบครัว เมื่อลูกสาวที่ทำงานต่างจังหวัดได้รับสายด่วนจาก “แม่” ที่กำลังตกอยู่ในอันตรายและต้องการเงินฉุกเฉิน น้ำเสียง ความสั่นเครือ และคำพูดที่ใช้เป็นแม่ของเธอทุกประการ แต่เมื่อเรื่องราวคลี่คลาย ผู้ชมจะพบความจริงที่น่าขนลุกว่าเสียงทั้งหมดถูกสร้างขึ้นจาก AI Voice Cloning โดยมิจฉาชีพที่ดึงข้อมูลจากโซเชียลมีเดีย เรื่องนี้เล่นกับความรู้สึกพื้นฐานที่สุดของมนุษย์—ความรักและความห่วงใยที่ถูกนำมาเป็นอาวุธ`,
  keyMessage: `เทคโนโลยีเลียนแบบได้ทุกอย่าง แม้กระทั่งคนที่คุณรัก... มีเพียง “สติ” เท่านั้นที่ AI ปลอมไม่ได้ (AI can fake anything, even your loved ones. The only thing it can't fake is your awareness.)`,
  targetAudience: `กลุ่มหลัก: วัยทำงานอายุ 25–45 ปี ที่ดูแลหรือส่งเงินให้พ่อแม่ซึ่งอยู่ต่างที่ และเสี่ยงต่อการหลอกลวงที่อ้างถึงบุคคลในครอบครัว\n\nกลุ่มรอง: ผู้สูงอายุอายุ 55 ปีขึ้นไป เพื่อให้ตระหนักว่าเสียงและภาพของตนสามารถถูกนำไปใช้หลอกลูกหลานได้`,
  callToAction: `หยุด เอ๊ะ และโทรเช็กเบอร์จริงทุกครั้งก่อนโอน — ใช้ “รหัสลับครอบครัว” หรือโทรกลับเบอร์เดิมที่บันทึกไว้เสมอเมื่อมีการขอความช่วยเหลือฉุกเฉินเรื่องเงิน`,
  genre: 'Psychological Thriller / Family Drama — ระทึกขวัญเชิงจิตวิทยาผสมดราม่าครอบครัว',
  tone: `ช่วงต้น: อบอุ่น คุ้นเคย เพื่อสร้างความไว้ใจ\nช่วงกลาง: บีบคั้น ตื่นตระหนก เร่งรีบ\nช่วงท้าย: หักมุม เย็นเยียบ น่าขนลุก ก่อนจบอย่างหนักแน่นเพื่อเตือนสติ`,
  visualStyle: `Contrast of Realities — ตัดสลับสองโลก\nโลกของเหยื่อ: แสงโทนอุ่น สีส้ม/เหลือง สื่อความผูกพันและห่วงใย\nโลกของมิจฉาชีพ: แสงโทนเย็น ฟ้า/เขียวเข้ม แสงจากจอคอมพิวเตอร์ คลื่นเสียงและโค้ด AI\nCamera Work: Slow Push-in เข้าหาใบหน้าระหว่างความตื่นตระหนก เพื่อสร้างความอึดอัดและแรงกดดัน`,
  constraints: `ห้ามทำให้เหยื่อดูโง่: เหยื่อหลงเชื่อเพราะความรักและ AI แนบเนียน ไม่ใช่เพราะประมาท เพื่อให้ผู้ชมเห็นอกเห็นใจและตระหนักว่าเกิดกับใครก็ได้\n\nหลีกเลี่ยงศัพท์เทคนิคซับซ้อน: เน้นผลกระทบของ Voice Cloning มากกว่ากระบวนการเชิงลึก\n\nไม่มีภาพความรุนแรง: เน้นผลกระทบทางจิตวิทยาแทนความรุนแรงทางกายภาพ`
});
const CHARACTER_SHEET_STYLES = Object.freeze(['Cinematic Realism','Photorealistic','3D Character Concept','3D Cartoon','Chibi','Shibi / Super-deformed','Anime / Manga','Stylized Illustration','Pencil Concept Art','Clay / Stop-motion','Low-poly 3D','Comic Book','Watercolor Illustration']);
const STORYBOARD_STYLES = Object.freeze(['Cinematic Color','Photorealistic','Concept Art','Anime / Manga','3D Previsualization','Pencil Storyboard','Black-and-white Ink']);

const esc = (value = '') => String(value).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const attr = esc;
const assetUrl = relative => relative ? `clip-asset://project/${String(relative).split('/').map(encodeURIComponent).join('/')}` : '';
const shots = () => (state.project?.scenes || []).flatMap((scene, si) => (scene.shots || []).map((shot, hi) => ({scene,shot,si,hi})));
const segments = () => shots().flatMap(x => (x.shot.videoSegments || []).map((segment, vi) => ({...x,segment,vi})));
const dialogues = () => shots().flatMap(x => (x.shot.dialogue || []).map((dialogue,di) => ({...x,dialogue,di})));
const uid = prefix => `${prefix}_${crypto.randomUUID()}`;
const duration = () => segments().reduce((n,x) => n + Number(x.segment.durationSec || 0), 0) || shots().reduce((n,x) => n + Number(x.shot.plannedDurationSec || 0), 0);
function characterReadiness(project=state.project) {
  const characters=project?.characters||[];
  const missingReference=characters.filter(character=>!character.referenceImages?.length);
  return {ready:Boolean(characters.length&&!missingReference.length),characters,missingReference};
}
function workflowSync(project=state.project){
  if(!project)return{briefRevision:0,changedFields:[],stale:{}};
  project.workflowSync||={briefRevision:0,briefChangedAt:'',changedFields:[],stale:{}};
  project.workflowSync.changedFields||=[];
  project.workflowSync.stale={characters:false,story:false,storyboard:false,video:false,voice:false,timeline:false,...(project.workflowSync.stale||{})};
  return project.workflowSync;
}
function markBriefChanged(path){
  const sync=workflowSync();sync.briefRevision=(Number(sync.briefRevision)||0)+1;sync.briefChangedAt=new Date().toISOString();
  if(!sync.changedFields.includes(path))sync.changedFields.push(path);
  const visualOnly=['brief.visualStyle','brief.aspectRatio','brief.storyboardStyle'].includes(path);
  const characterStyle=path==='brief.characterStyle';
  const hasCharacters=Boolean(state.project.characters?.length),hasStory=Boolean(state.project.scenes?.length||state.project.story?.synopsis||state.project.story?.title);
  if(characterStyle&&hasCharacters){sync.stale.characters=true;(state.project.characters||[]).forEach(character=>character.sheetStale=true);}
  else if(visualOnly&&hasStory)Object.assign(sync.stale,{storyboard:true,video:true,timeline:true});
  else if(!characterStyle){sync.stale.characters=hasCharacters;sync.stale.story=hasStory;if(hasStory)Object.assign(sync.stale,{storyboard:true,video:true,voice:true,timeline:true});}
  for(const {shot} of shots()){
    if(!characterStyle){shot.promptStale=true;shot.storyboardStale=true;}
    if(!characterStyle){shot.videoPromptStale=true;(shot.videoSegments||[]).forEach(segment=>{segment.promptStale=true;if(segment.videoClipRelativePath)segment.videoStale=true;});}
    if(!visualOnly&&!characterStyle)(shot.dialogue||[]).forEach(line=>{if(line.audioRelativePath)line.voiceStale=true;});
  }
}
function clearWorkflowStale(section){const sync=workflowSync();sync.stale[section]=false;if(!Object.values(sync.stale).some(Boolean))sync.changedFields=[];}
function staleBanner(section,actions=''){
  const sync=workflowSync();if(!sync.stale?.[section])return'';
  const fields=sync.changedFields.map(path=>path.replace(/^brief\./,'')).join(', ');
  return `<section class="card workflow-note stale-note"><div class="spread"><div><span class="badge warn">Brief เปลี่ยนแล้ว · revision ${sync.briefRevision}</span><strong>ควรอัปเดตส่วนนี้ตาม Brief ล่าสุด</strong><p>ค่าที่เปลี่ยน: ${esc(fields||'ชื่อโปรเจกต์')} · งานเดิมยังถูกเก็บไว้และทำเครื่องหมาย stale</p></div><div class="row wrap">${actions}</div></div></section>`;
}
function routeIsStale(route){const map={characters:'characters',story:'story',storyboard:'storyboard',voice:'voice',timeline:'timeline',export:'timeline'};return Boolean(workflowSync().stale?.[map[route]]);}
function friendlyError(error={}) {
  const code=error.code||'error', raw=error.message||'เกิดข้อผิดพลาด';
  if (['network','timeout'].includes(code)||/network offline|timed out|unreachable/i.test(raw)) return { title:'เชื่อมต่อ AI ไม่สำเร็จ', message:code==='timeout'?'Vendor ตอบกลับช้ากว่าเวลาที่กำหนด':'แอปติดต่อ AI vendor ไม่ได้', code, hints:['เปิด Settings → AI Vendor Registry แล้วกด Test Connection','ตรวจว่า API key, endpoint, ภูมิภาคของ key และ model ID ตรงกับ vendor','ตรวจอินเทอร์เน็ต, VPN, Proxy หรือ Firewall ที่อาจบล็อกแอป','หากโมเดลตอบช้า ให้เพิ่ม AI request timeout แล้วลองใหม่'] };
  if(code==='authentication')return{title:'API key ไม่ถูกต้องหรือไม่มีสิทธิ์',message:raw,code,hints:['บันทึก API key ใหม่ใน Settings','ตรวจว่า key อยู่ภูมิภาคเดียวกับ endpoint โดยเฉพาะ Qwen']};
  if(code==='rate_limit')return{title:'เกินโควตาหรือถูกจำกัดอัตรา',message:raw,code,hints:['รอสักครู่แล้วลองใหม่','ตรวจเครดิตและ quota ที่หน้า vendor']};
  if(code==='malformed_response')return{title:'AI ตอบกลับไม่เป็น JSON ที่แอปรองรับ',message:raw,code,hints:['เลือก model ที่รองรับ structured/JSON output','เปิด JSON mode ของ vendor หรือเปลี่ยน model']};
  if(['ffmpeg_unavailable','ffmpeg_error'].includes(code))return{title:'ไม่สามารถเตรียม Video แบบไม่มีเสียง',message:raw,code,hints:['ไปที่ Settings แล้วกด Test FFmpeg','หากกำหนด FFmpeg path เอง ให้ตรวจว่าเป็นไฟล์ ffmpeg.exe ที่เปิดใช้งานได้','ติดตั้งแอปเวอร์ชันล่าสุดซึ่งรองรับ app.asar.unpacked']};
  if(code==='character_required')return{title:'ต้องกำหนดตัวละครก่อนสร้าง Story',message:raw,code,hints:['ไปที่หน้า “ตัวละคร” แล้วสร้างหรือเพิ่มตัวละคร','สร้าง Character Sheet หรือเพิ่มรูปอ้างอิงอย่างน้อย 1 รูปให้ตัวละครทุกตัว','Story จะใช้เฉพาะ Character ID รูปลักษณ์ เสื้อผ้า และสไตล์ที่กำหนดไว้']};
  if(code==='storyboard_required')return{title:'ต้องมีภาพ Storyboard ก่อน',message:raw,code,hints:['ไปที่หน้า Storyboard แล้วสร้างหรือ Import ภาพของ Shot นี้','Video Segment เป็น image-to-video และจะใช้ภาพนี้เป็น input_reference บังคับ','เมื่อภาพ Storyboard เปลี่ยน ให้สร้าง Segment ใหม่เพื่อให้ Prompt ตรงกับภาพล่าสุด']};
  if(code==='content_policy')return{title:'Sora ปฏิเสธ Prompt หรือภาพอ้างอิงด้วยระบบความปลอดภัย',message:raw,code,details:error.details,hints:['กด “ปรับ Prompt สำหรับ Sora” เพื่อเปลี่ยนเป็นสื่อรณรงค์ที่ใช้ตัวละครสมมติและตัดรายละเอียดเชิงปฏิบัติ','หลีกเลี่ยงบุคคลจริง บุคคลสาธารณะ การเลียนแบบใบหน้า/เสียง ข้อมูลส่วนตัว ข้อมูลธนาคาร และขั้นตอนการหลอกลวง','หากยังถูกบล็อก สาเหตุอาจมาจากภาพ Storyboard ที่ดูเหมือนบุคคลจริง—สร้างภาพใหม่ด้วย 3D Cartoon, Chibi, Anime หรือ Concept Art','ระบบความปลอดภัยเป็นการตัดสินของ provider การปรับ Prompt จึงช่วยลดความเสี่ยงแต่ไม่รับประกันว่าจะผ่าน']};
  return {title:'เกิดข้อผิดพลาด',message:raw,code,hints:[]};
}
const invoke = async (promise, busy = 'กำลังทำงาน…') => {
  state.busyAction=state.pendingAction; state.busy=busy||'กำลังทำงาน…'; state.error=''; render();
  let response;
  try { response=await promise; }
  catch(error){ response={ok:false,error:{message:error.message,code:'error'}}; }
  state.busy=''; state.busyAction='';
  if (!response?.ok) { state.error=friendlyError(response?.error); render(); return null; }
  return response.data;
};
function toast(message) { const el=document.createElement('div'); el.className='toast'; el.textContent=message; toastRegion.append(el); setTimeout(()=>el.remove(),3200); }
function media(path, type='image', vertical=false, revision='') {
  if (!path) return `<div class="media-frame ${vertical?'vertical':''}"><span>ยังไม่มีไฟล์</span></div>`;
  const source=`${assetUrl(path)}${revision?`?v=${encodeURIComponent(revision)}`:''}`;
  if (type==='video') return `<div class="media-frame video-preview ${vertical?'vertical-video':''}"><video controls playsinline preload="metadata" src="${attr(source)}"></video></div>`;
  if (type==='audio') return `<div class="media-frame"><audio controls src="${attr(source)}"></audio></div>`;
  return `<div class="media-frame ${vertical?'vertical':''}"><img src="${attr(source)}" alt="preview"></div>`;
}
function field(label, path, value, options={}) {
  const tag=options.tag||'input'; const common=`data-path="${attr(path)}" ${options.type?`type="${options.type}"`:''} ${options.min!==undefined?`min="${options.min}"`:''} ${options.max!==undefined?`max="${options.max}"`:''}`;
  const control = tag==='textarea' ? `<textarea ${common} rows="${options.rows||3}" placeholder="${attr(options.placeholder||'')}">${esc(value)}</textarea>` : tag==='select' ? `<select ${common}>${options.items.map(item=>{const optionValue=typeof item==='object'?item.value:item,optionLabel=typeof item==='object'?item.label:item;return `<option value="${attr(optionValue)}" ${String(value)===String(optionValue)?'selected':''}>${esc(optionLabel)}</option>`;}).join('')}</select>` : `<input ${common} value="${attr(value??'')}" placeholder="${attr(options.placeholder||'')}">`;
  return `<div class="field ${options.class||''}"><label>${esc(label)}</label>${control}${options.help?`<small>${esc(options.help)}</small>`:''}</div>`;
}
function characterStyleField(label, value, options={}) {
  const binding=options.path?`data-path="${attr(options.path)}"`:options.name?`name="${attr(options.name)}"`:'';
  return `<div class="field"><label>${esc(label)}</label><input ${binding} list="character-style-presets" value="${attr(value||'Cinematic Realism')}" placeholder="เช่น 3D Cartoon, Chibi หรือพิมพ์สไตล์เอง"><small>${esc(options.help||'เลือก preset หรือพิมพ์รูปแบบภาพของคุณเองได้')}</small></div>`;
}
function characterStyleDatalist(){return `<datalist id="character-style-presets">${CHARACTER_SHEET_STYLES.map(style=>`<option value="${attr(style)}"></option>`).join('')}</datalist>`;}
function button(label, action, cls='', data='') { const active=state.busyAction===action; return `<button class="btn ${cls} ${active?'is-busy':''}" data-action="${action}" ${data} ${active?'disabled aria-busy="true"':''}>${active?`<span class="spinner"></span>${esc(state.busy||'กำลังทำงาน…')}`:label}</button>`; }
function errorZone() { if(!state.error)return'';const error=typeof state.error==='string'?friendlyError({message:state.error}):state.error;return `<div class="error-zone"><div><strong>${esc(error.title)}</strong><p>${esc(error.message)}</p>${error.hints?.length?`<ul>${error.hints.map(h=>`<li>${esc(h)}</li>`).join('')}</ul>`:''}<div class="row">${['network','timeout','authentication'].includes(error.code)?button('ไปที่ Settings','go-settings','small'):''}${button('ปิด','clear-error','ghost small')}</div></div></div>`; }

function shell(content) {
  const route=state.route; const routeLabel=navItems.find(x=>x[0]===route)?.[1];
  return `<div class="shell"><aside class="sidebar">
    <div class="brand"><div class="brand-mark">CS</div><div><strong>Clip Story</strong><small>Studio</small></div></div>
    <nav class="nav">${navItems.map(([key,label],i)=>`<button class="nav-btn ${route===key?'active':''}" data-nav="${key}" ${!state.project&&!['projects','settings'].includes(key)?'disabled':''}><span class="nav-index">${i+1}</span><span>${label}${state.project&&routeIsStale(key)?' <b class="stale-dot" title="Brief เปลี่ยนแล้ว">●</b>':''}</span></button>`).join('')}</nav>
    <div class="side-bottom"><div class="project-mini">${esc(state.project?.title||'ยังไม่เปิดโปรเจกต์')}</div><div class="save-pill" id="save-status">● ${esc(state.saveStatus)}</div></div>
  </aside><main class="main"><header class="topbar"><div class="breadcrumbs">Clip Story Studio / <strong>${esc(routeLabel)}</strong></div><div class="top-actions">${state.project?button('บันทึก','save','small'):''}${button('⚙','go-settings','small ghost')}</div></header>
  ${state.busy?`<div class="activity-bar"><span class="spinner"></span><strong>${esc(state.busy)}</strong><div class="activity-track"><i></i></div><small>กรุณารอและอย่าปิดแอป</small></div>`:''}
  <section class="content">${errorZone()}${content}</section></main>
  ${state.project?`<div class="sticky-bar"><div class="undo">${state.undo?`ลบแล้ว: ${esc(state.undo.label)} ${button('Undo','undo','small ghost')}`:`ความยาวปัจจุบัน ${duration()} วินาที · เป้าหมาย ${state.project.brief.targetDurationSec} วินาที`}</div><span class="badge ${state.saveStatus==='บันทึกแล้ว'?'ok':''}" id="bottom-status">${esc(state.saveStatus)}</span></div>`:''}</div>`;
}
function pageHead(title, description, actions='') { return `<div class="page-head"><div><h1>${title}</h1><p>${description}</p></div><div class="row wrap">${actions}</div></div>`; }

function projectsView() {
  const cards=state.recents.map(item=>`<article class="card project-card"><div class="project-cover"></div><div class="spread"><h3>${esc(item.title)}</h3><span class="badge">${esc(item.aspectRatio)}</span></div><p>${item.targetDurationSec}s · แก้ไข ${new Date(item.updatedAt).toLocaleDateString('th-TH')}</p><div class="row actions">${button('เปิด','open-recent','primary small',`data-root="${attr(item.root)}"`)}${button('เอาออกจากรายการ','archive-recent','ghost small',`data-root="${attr(item.root)}"`)}</div></article>`).join('');
  const p=state.project, allShots=p?shots():[], allDialogues=p?dialogues():[];
  const workflow=p?[['1','Project Dashboard',true,'go-dashboard','ภาพรวมโปรเจกต์และสถานะงาน'],['2','บรีฟ',Boolean(p.brief?.concept),'go-brief','กำหนดสิ่งที่ต้องการ'],['3','ตัวละคร',characterReadiness(p).ready,'go-characters','เพศ อายุ บุคลิก รูปและเสียง'],['4','เรื่องราว',Boolean(p.scenes?.length),'go-story','ฉาก เหตุการณ์ กล้อง และบทพูด'],['5','Storyboard',Boolean(allShots.length&&allShots.every(item=>item.shot.storyboardImageRelativePath)),'go-storyboard','ภาพใหม่ที่อ้างอิง Character Sheet'],['6','เสียง',Boolean(allDialogues.length&&allDialogues.every(item=>item.dialogue.audioRelativePath)),'go-voice','พากย์ตามบุคลิกตัวละคร']]:[];
  return `${pageHead('Project Dashboard','ภาพรวมกระบวนการผลิตและสถานะของทุกโปรเจกต์ในเครื่อง',button('+ โปรเจกต์ใหม่','new-project','primary')+button('เปิดโฟลเดอร์','open-project'))}
  ${state.project?`<div class="card spread" style="margin-bottom:18px"><div><span class="badge ok">กำลังทำงาน</span><h2>${esc(state.project.title)}</h2><p>${esc(state.project.brief.platform)} · ${esc(state.project.brief.aspectRatio)} · ${duration()}s</p></div><div class="row">${button('ทำสำเนา','duplicate-project')}${button('ไปที่ Brief','go-brief','primary')}</div></div>`:''}
  ${p?`<section class="card"><div class="spread"><div><h2>Production workflow 1–6</h2><p>Timeline และ Export เป็นขั้นประกอบ/ส่งมอบหลังสร้างสื่อ</p></div><span class="badge ${segments().some(item=>item.segment.videoClipRelativePath)?'ok':'warn'}">Video ${segments().filter(item=>item.segment.videoClipRelativePath).length}/${segments().length}</span></div><div class="grid three">${workflow.map(([number,label,ready,action,description])=>`<button class="card inset workflow-step" data-action="${action}"><div class="spread"><span class="shot-number">STEP ${number}</span><span class="badge ${ready?'ok':'warn'}">${ready?'พร้อม':'รอดำเนินการ'}</span></div><h3>${label}</h3><p>${description}</p></button>`).join('')}</div></section>`:''}
  ${cards?`<div class="grid three">${cards}</div>`:`<div class="empty"><span class="icon">◫</span><h2>เริ่มเรื่องแรกของคุณ</h2><p>สร้างโปรเจกต์ใหม่หรือเปิดโฟลเดอร์ที่มี project.json</p></div>`}`;
}
function briefView() { const p=state.project,b=p.brief; return `${pageHead('Creative Brief','วางแก่นเรื่องและข้อจำกัดให้ชัด แล้วกำหนดตัวละครก่อนสร้าง Story',button('เติมข้อมูลตัวอย่าง','fill-brief-example')+button('สร้างตัวละครต่อ','go-characters','primary'))}
  <div class="example-banner"><div><span class="badge violet">ตัวอย่าง Brief</span><strong>เสียงคุ้นหู... ผู้แปลกหน้า (Familiar Stranger)</strong><p>เรื่องเตือนภัย AI Voice Cloning ที่ใช้ความรักและความห่วงใยเป็นอาวุธ</p></div>${button('ใช้ตัวอย่างนี้','fill-brief-example','small cyan')}</div>
  ${Object.values(workflowSync(p).stale).some(Boolean)?`<section class="card workflow-note stale-note"><div class="spread"><div><span class="badge warn">Brief revision ${workflowSync(p).briefRevision}</span><strong>มีส่วนงานที่ต้องอัปเดตตาม Brief ล่าสุด</strong><p>${Object.entries(workflowSync(p).stale).filter(([,value])=>value).map(([key])=>key).join(' · ')}</p></div><div class="row wrap">${button('ไปตรวจตัวละคร','go-characters','small')}${characterReadiness(p).ready?button('สร้าง Story ใหม่ตาม Brief','generate-story','primary small'):''}</div></div></section>`:''}
  <div class="grid two"><section class="card">${field('ชื่อโปรเจกต์','title',p.title,{placeholder:'เช่น เสียงคุ้นหู... ผู้แปลกหน้า'})}${field('Concept / เนื้อหาต้นทาง','brief.concept',b.concept,{tag:'textarea',rows:7,placeholder:'เล่าเหตุการณ์ จุดหักมุม และความรู้สึกหลักของเรื่อง…'})}${field('Key message','brief.keyMessage',b.keyMessage,{tag:'textarea',rows:4,placeholder:'เช่น เทคโนโลยีเลียนแบบได้ทุกอย่าง แต่สติปลอมไม่ได้'})}${field('กลุ่มเป้าหมาย','brief.targetAudience',b.targetAudience,{tag:'textarea',rows:5,placeholder:'กลุ่มหลัก กลุ่มรอง อายุ พฤติกรรม และความเสี่ยง'})}${field('Call to action','brief.callToAction',b.callToAction,{tag:'textarea',rows:4,placeholder:'สิ่งที่ต้องการให้ผู้ชมทำหลังดูจบ'})}</section>
  <section class="card"><div class="grid two">${field('Platform','brief.platform',b.platform,{tag:'select',items:['TikTok','Reels','Shorts','YouTube','Custom']})}${field('Aspect ratio','brief.aspectRatio',b.aspectRatio,{tag:'select',items:['9:16','16:9','1:1']})}${field('ความยาวเป้าหมาย (วินาที)','brief.targetDurationSec',b.targetDurationSec,{type:'number',min:1,max:7200})}${field('ภาษา','brief.language',b.language,{tag:'select',items:['ไทย','English','日本語','中文']})}</div>${field('Genre','brief.genre',b.genre,{placeholder:'เช่น Psychological Thriller / Family Drama'})}${field('Tone / ลำดับอารมณ์','brief.tone',b.tone,{tag:'textarea',rows:5,placeholder:'ช่วงต้น → ช่วงกลาง → ช่วงท้าย'})}${field('Visual style','brief.visualStyle',b.visualStyle,{tag:'textarea',rows:7,placeholder:'แสง สี องค์ประกอบ และ Camera Work'})}${field('รูปแบบภาพ Storyboard','brief.storyboardStyle',b.storyboardStyle||'Cinematic Color',{tag:'select',items:STORYBOARD_STYLES,help:'เลือกภาพสี ภาพสมจริง Concept Art, Anime, 3D หรือขาวดำได้'})}${field('ข้อจำกัด / สิ่งที่ไม่ต้องการ','brief.constraints',b.constraints,{tag:'textarea',rows:7,placeholder:'สิ่งที่ต้องหลีกเลี่ยง ความปลอดภัย และข้อกำหนดด้านเนื้อหา'})}</section></div>`; }

function storyView() {
  const p=state.project; const summary=p.story; const readiness=characterReadiness(p);
  const sceneHtml=(p.scenes||[]).map((scene,si)=>{const sceneShots=scene.shots||[],promptCount=sceneShots.filter(shot=>shot.imagePrompt).length,imageCount=sceneShots.filter(shot=>shot.storyboardImageRelativePath).length;return `<section class="card scene"><div class="spread scene-head"><div><div class="row"><span class="shot-number">SCENE ${scene.sceneNumber}</span><span class="badge ${promptCount===sceneShots.length&&sceneShots.length?'ok':'warn'}">Prompt ${promptCount}/${sceneShots.length}</span><span class="badge ${imageCount===sceneShots.length&&sceneShots.length?'ok':'warn'}">Storyboard ${imageCount}/${sceneShots.length}</span></div><h2>${esc(scene.title)}</h2><p>${esc(scene.purpose)}</p></div><div class="row">${button(scene.locked?'🔒 ล็อกแล้ว':'🔓 ล็อก','lock-scene','small',`data-si="${si}"`)}${button('↑','move-scene-up','small ghost',`data-si="${si}"`)}${button('↓','move-scene-down','small ghost',`data-si="${si}"`)}${button('ลบ','delete-scene','danger small',`data-si="${si}"`)}</div></div>
    <div class="grid two" style="margin-top:15px">${field('ชื่อฉาก',`scenes.${si}.title`,scene.title)}${field('จุดประสงค์ / เหตุการณ์',`scenes.${si}.purpose`,scene.purpose)}${field('สถานที่',`scenes.${si}.location`,scene.location)}${field('ช่วงเวลา',`scenes.${si}.timeOfDay`,scene.timeOfDay)}${field('รายละเอียดสถานที่',`scenes.${si}.settingDescription`,scene.settingDescription||'',{tag:'textarea'})}${field('บรรยากาศ',`scenes.${si}.atmosphere`,scene.atmosphere||'',{tag:'textarea'})}${field('Mood',`scenes.${si}.mood`,scene.mood||'')}${field('Tone',`scenes.${si}.tone`,scene.tone||'')}${field('Narrative beat',`scenes.${si}.narrativeBeat`,scene.narrativeBeat||'',{tag:'textarea'})}${field('Emotional arc',`scenes.${si}.emotionalArc`,scene.emotionalArc||'',{tag:'textarea'})}</div>
    ${sceneShots.map((shot,hi)=>`<div class="shot"><div class="spread"><div class="row"><span class="shot-number">SHOT ${shot.shotNumber}</span><strong>${shot.plannedDurationSec}s</strong><span class="badge ${shot.promptStale?'warn':shot.imagePrompt?'ok':'warn'}">${shot.promptStale?'Prompt เก่ากว่า Brief':shot.imagePrompt?'✓ Prompt พร้อม':'ยังไม่มี Prompt'}</span>${shot.storyboardImageRelativePath?`<span class="badge ${shot.storyboardStale?'warn':'ok'}">${shot.storyboardStale?'Storyboard ต้องอัปเดต':'✓ Storyboard'}</span>`:''}${shot.videoSegments?.length?`<span class="badge ${shot.videoPromptStale?'warn':'violet'}">Segments ${shot.videoSegments.length}${shot.videoPromptStale?' · stale':''}</span>`:''}</div><div class="row">${button(shot.locked?'🔒':'🔓','lock-shot','small ghost',`data-si="${si}" data-hi="${hi}"`)}${button('↑','move-shot-up','small ghost',`data-si="${si}" data-hi="${hi}"`)}${button('↓','move-shot-down','small ghost',`data-si="${si}" data-hi="${hi}"`)}${button('ลบ','delete-shot','danger small',`data-si="${si}" data-hi="${hi}"`)}</div></div>
      <div class="grid two" style="margin-top:12px">${field('รายละเอียด',`scenes.${si}.shots.${hi}.description`,shot.description,{tag:'textarea'})}${field('Action',`scenes.${si}.shots.${hi}.action`,shot.action,{tag:'textarea'})}${field('เวลา (วินาที)',`scenes.${si}.shots.${hi}.plannedDurationSec`,shot.plannedDurationSec,{type:'number',min:1,max:3600})}${field('Camera movement',`scenes.${si}.shots.${hi}.camera.movement`,shot.camera?.movement||'')}${field('รูปแบบภาพ Storyboard',`scenes.${si}.shots.${hi}.storyboardStyle`,shot.storyboardStyle||p.brief.storyboardStyle||'Cinematic Color',{tag:'select',items:STORYBOARD_STYLES})}</div>
      <label class="field"><span>ตัวละครใน Shot (อ้างอิง Character Bible เท่านั้น)</span><span class="character-picker">${p.characters.map((character,ci)=>`<button type="button" class="character-chip ${(shot.characters||[]).includes(character.id)?'selected':''}" data-action="toggle-shot-character" data-si="${si}" data-hi="${hi}" data-ci="${ci}">${esc(character.name)} · ${esc(character.sheetStyle||'Cinematic Realism')}</button>`).join('')}</span></label>
      ${shot.storyboardImageRelativePath?`<div style="margin:12px 0;max-width:520px">${media(shot.storyboardImageRelativePath,'image',p.brief.aspectRatio==='9:16')}</div>`:''}
      ${(shot.dialogue||[]).map((line,di)=>`<div class="grid three"><div>${field('ผู้พูด',`scenes.${si}.shots.${hi}.dialogue.${di}.speakerId`,line.speakerId,{tag:'select',items:['narrator',...p.characters.map(character=>character.id)]})}</div><div>${field('ผู้ฟัง (Character IDs คั่นด้วย comma)',`scenes.${si}.shots.${hi}.dialogue.${di}.listenerIds`,(line.listenerIds||[]).join(','))}</div><div>${field('อารมณ์',`scenes.${si}.shots.${hi}.dialogue.${di}.emotion`,line.emotion||'neutral')}</div><div>${field('น้ำเสียง / วิธีส่งคำ',`scenes.${si}.shots.${hi}.dialogue.${di}.delivery`,line.delivery||'natural')}</div><div class="span-2">${field('บทพูด / บรรยาย',`scenes.${si}.shots.${hi}.dialogue.${di}.text`,line.text,{tag:'textarea'})}</div></div>`).join('')}
      <div class="row wrap">${button('+ บทพูด','add-dialogue','small',`data-si="${si}" data-hi="${hi}"`)}${button(shot.imagePrompt?'สร้าง Prompt ใหม่':'สร้าง Prompt','prompt-shot',shot.imagePrompt?'small ghost':'small cyan',`data-si="${si}" data-hi="${hi}"`)}${button(shot.storyboardImageRelativePath?'สร้างภาพ Storyboard ใหม่':'สร้างภาพ Storyboard','generate-storyboard-image',shot.storyboardImageRelativePath?'small ghost':'small primary',`data-si="${si}" data-hi="${hi}"`)}</div></div>`).join('')}
    <div style="margin-top:14px">${button('+ Shot','add-shot','small',`data-si="${si}"`)}</div></section>`;}).join('');
  return `${pageHead('Story Editor','สร้างฉาก เหตุการณ์ บรรยากาศ กล้อง บทพูด และ Prompt ภาพร่าง โดยอ้างอิง Character Bible เท่านั้น',button(state.busy?'กำลังสร้าง…':'สร้าง Story','generate-story','primary',state.busy||!readiness.ready?'disabled title="สร้าง Character Sheet ให้ครบก่อน"':'')+button('Export Prompt สำหรับ AI ภายนอก','export-external-storyboard-prompt','cyan')+button('สร้างภาพทุก Shot','generate-all-storyboard-images','cyan')+button('+ Scene','add-scene'))}
  ${staleBanner('story',readiness.ready?button('สร้าง Story ใหม่ตาม Brief','generate-story','primary small'):'')}
  <section class="card workflow-note"><div class="spread"><div><strong>${readiness.ready?'✓ Character Bible พร้อม':'ต้องเตรียม Character Bible ก่อน'}</strong><p>${readiness.ready?`พร้อมใช้ตัวละคร ${readiness.characters.length} ตัวใน Story และ Prompt ทุก Shot`:`ยังขาดรูปอ้างอิง: ${readiness.missingReference.map(character=>character.name).join(', ')||'ยังไม่มีตัวละคร'}`}</p></div>${button('ไปที่ตัวละคร','go-characters','small cyan')}</div></section>
  <section class="card story-summary"><div class="grid two">${field('ชื่อเรื่อง','story.title',summary.title)}${field('Hook','story.hook',summary.hook)}${field('Logline','story.logline',summary.logline,{tag:'textarea'})}${field('Opening','story.opening',summary.opening||'',{tag:'textarea'})}${field('Climax','story.climax',summary.climax||'',{tag:'textarea'})}${field('Ending','story.ending',summary.ending,{tag:'textarea'})}<div class="span-2">${field('Synopsis','story.synopsis',summary.synopsis,{tag:'textarea',rows:4})}</div></div></section>
  ${sceneHtml||`<div class="empty"><span class="icon">✦</span><h2>ยังไม่มี Story</h2><p>กรอก Brief แล้วให้ Mock AI สร้างโครงเรื่องเพื่อเริ่มแก้ไข</p>${button('สร้าง Story','generate-story','primary')}</div>`}`;
}

function charactersView() {
  const list=(state.project.characters||[]).map((c,ci)=>{const primary=c.referenceImages?.find(x=>x.isPrimary)||c.referenceImages?.[0],hasSheet=c.referenceImages?.some(image=>image.source==='ai-character-sheet');return `<article class="card character-card"><div class="spread"><div class="row"><div class="avatar">${primary?`<img class="avatar" src="${attr(assetUrl(primary.relativePath))}" alt="">`:'♙'}</div><div><h2>${esc(c.name)}</h2><span class="badge violet">${esc(c.role||'ตัวละคร')}</span> <span class="badge">${esc([c.gender,c.lifeStage,c.ageYears?`${c.ageYears} ปี`:c.ageRange].filter(Boolean).join(' · ')||'ยังไม่ระบุ demographic')}</span> ${c.sheetStale?'<span class="badge warn">ต้องสร้าง Sheet ใหม่</span>':hasSheet?'<span class="badge ok">✓ Character Sheet</span>':''}</div></div><div>${button('แก้ไข','edit-character','small',`data-ci="${ci}"`)}</div></div>${primary?media(primary.relativePath,'image'):''}<p>${esc(c.appearance||'ยังไม่มีคำอธิบายรูปลักษณ์')}</p><p><strong>บุคลิก:</strong> ${esc(c.personality||'ยังไม่ระบุ')}<br><strong>เสียง:</strong> ${esc([c.speakingStyle,c.voiceProfile?.tone,c.voiceProfile?.pace].filter(Boolean).join(' · ')||'ยังไม่ระบุ')}</p><div class="prompt-box">${esc(c.visualConsistencyPrompt||'ยังไม่มี consistency prompt')}</div>${characterStyleField('รูปแบบภาพตัวละคร',c.sheetStyle||state.project.brief.characterStyle||'Cinematic Realism',{path:`characters.${ci}.sheetStyle`,help:'เลือก preset เช่น 3D Cartoon, Chibi, Shibi หรือพิมพ์สไตล์ใหม่ได้เอง'})}<div class="row wrap" style="margin-top:14px">${button(hasSheet?'สร้าง Character Sheet ใหม่':'สร้าง Character Sheet AI','generate-character-sheet','primary small',`data-ci="${ci}"`)}${button('+ รูปอ้างอิง','import-character-image','small',`data-ci="${ci}"`)}${button('ทำสำเนา','duplicate-character','small ghost',`data-ci="${ci}"`)}${button('ลบ','delete-character','danger small',`data-ci="${ci}"`)}</div>${(c.referenceImages||[]).length?`<div class="row wrap" style="margin-top:12px">${c.referenceImages.map((img,ii)=>`<button class="icon-btn" title="${esc(img.caption||'ตั้งเป็นภาพหลัก')}" data-action="primary-character-image" data-ci="${ci}" data-ii="${ii}"><img src="${attr(assetUrl(img.relativePath))}" style="width:45px;height:45px;border-radius:9px;object-fit:cover;outline:${img.isPrimary?'2px solid #8b5cf6':'none'}"></button>`).join('')}</div>`:''}</article>`}).join('');
  const readiness=characterReadiness(state.project);
  const actions=button('สร้าง Sheet ที่ขาด/ต้องอัปเดต','generate-all-character-sheets','primary')+button('✦ AI สร้างตัวละคร','generate-characters')+button('Export System + User Prompt','export-external-character-prompt','cyan')+button('+ เพิ่มเอง','add-character')+button('สร้าง Story ต่อ','generate-story','cyan',readiness.ready?'':'disabled title="สร้าง Sheet หรือเพิ่มรูปอ้างอิงให้ครบก่อน"');
  return `${characterStyleDatalist()}${pageHead('Character Library','กำหนด Character Bible และภาพอ้างอิงก่อนสร้าง Story เพื่อรักษาตัวตนและสไตล์ทุกช็อต',actions)}${staleBanner('characters',button('ใช้ตัวละครเดิม','accept-characters','small')+button('ใช้ Style เริ่มต้นกับทุกตัว','apply-character-style-all','cyan small'))}<section class="card workflow-note"><div class="grid two"><div><strong>${readiness.ready?'✓ พร้อมสร้าง Story':'ขั้นตอนบังคับก่อน Story'}</strong><p>${readiness.ready?`ตัวละคร ${readiness.characters.length} ตัวมีรูปอ้างอิงครบแล้ว`:`สร้างตัวละครและเพิ่ม Character Sheet/รูปอ้างอิงให้ครบทุกตัว${readiness.missingReference.length?`: ${readiness.missingReference.map(character=>character.name).join(', ')}`:''}`}</p></div>${characterStyleField('รูปแบบเริ่มต้นสำหรับตัวละครใหม่',state.project.brief.characterStyle||'Cinematic Realism',{path:'brief.characterStyle',help:'AI สร้างตัวละครชุดใหม่จะใช้สไตล์นี้; แต่ละตัวปรับแยกได้ภายหลัง'})}</div></section>${list?`<div class="grid three">${list}</div>`:`<div class="empty"><span class="icon">✦</span><h2>ยังไม่มีตัวละคร</h2><p>เลือกสไตล์เริ่มต้น แล้วกด “AI สร้างตัวละคร” จากนั้นสร้าง Character Sheet ก่อนเริ่ม Story</p><div class="row" style="justify-content:center">${button('✦ AI สร้างตัวละคร','generate-characters','primary')}${button('+ เพิ่มเอง','add-character')}</div></div>`}`;
}

function storyboardView() {
  const cards=shots().map(({scene,shot,si,hi})=>{const imageLabel=shot.storyboardStale?'Storyboard เก่ากว่า Brief':shot.promptStale?'Prompt เก่ากว่า Brief':shot.storyboardImageRelativePath?(shot.imageGenerationMeta?'AI Storyboard พร้อม':'Image Imported'):shot.imagePrompt?'Prompt Ready':'Draft';return `<article class="card"><div class="spread"><div><span class="shot-number">SCENE ${scene.sceneNumber} / SHOT ${shot.shotNumber}</span><h2>${esc(scene.title)}</h2></div><div><span class="badge ${shot.storyboardStale||shot.promptStale?'warn':shot.storyboardImageRelativePath?'ok':shot.imagePrompt?'violet':'warn'}">${imageLabel}</span> <span class="badge">${shot.plannedDurationSec}s</span></div></div>
    <div class="grid two" style="margin-top:16px"><div>${media(shot.storyboardImageRelativePath,'image',state.project.brief.aspectRatio==='9:16')}<div class="row wrap" style="margin-top:10px">${button(shot.storyboardImageRelativePath?'สร้างภาพ AI ใหม่':'สร้างภาพ Storyboard AI','generate-storyboard-image','primary small',`data-si="${si}" data-hi="${hi}"`)}${button(shot.storyboardImageRelativePath?'Replace Import':'Import Image','import-storyboard','small',`data-si="${si}" data-hi="${hi}"`)}${shot.storyboardImageRelativePath?button('Remove','remove-storyboard','danger small',`data-si="${si}" data-hi="${hi}" data-path="${attr(shot.storyboardImageRelativePath)}"`):''}</div><small>เลือกสร้างได้ทั้งภาพสี ภาพสมจริง Concept Art, Anime, 3D หรือขาวดำ และอาจมีค่าใช้จ่ายตาม API</small></div>
    <div>${field('รูปแบบภาพ Storyboard',`scenes.${si}.shots.${hi}.storyboardStyle`,shot.storyboardStyle||state.project.brief.storyboardStyle||'Cinematic Color',{tag:'select',items:STORYBOARD_STYLES})}${shot.imageGenerationMeta?.referenceCount?`<div class="prompt-box"><strong>Character reference ที่ใช้จริง:</strong> ${shot.imageGenerationMeta.referenceCount} ตัว · ${esc((shot.imageGenerationMeta.referenceCharacterIds||[]).map(id=>state.project.characters.find(character=>character.id===id)?.name||id).join(', '))}</div>`:''}${field('Image prompt',`scenes.${si}.shots.${hi}.imagePrompt`,shot.imagePrompt,{tag:'textarea',rows:7})}${field('Negative prompt',`scenes.${si}.shots.${hi}.imageNegativePrompt`,shot.imageNegativePrompt,{tag:'textarea',rows:3})}<div class="row wrap">${button(shot.imagePrompt?'สร้าง Prompt ใหม่':'✦ สร้าง Prompt','prompt-shot',shot.imagePrompt?'ghost small':'cyan small',`data-si="${si}" data-hi="${hi}"`)}${button('Copy prompt','copy-shot-prompt','small',`data-si="${si}" data-hi="${hi}"`)}${button(shot.videoSegments?.length?'สร้าง Image-to-Video Segments ใหม่':'สร้าง Image-to-Video Segments','generate-segments','primary small',`data-si="${si}" data-hi="${hi}" ${shot.storyboardImageRelativePath?'':'disabled title="สร้างหรือ Import ภาพ Storyboard ก่อน"'}`)}</div><small>${shot.storyboardImageRelativePath?'พร้อมใช้ภาพ Storyboard ด้านซ้ายเป็น input_reference':'ต้องมีภาพ Storyboard ก่อนสร้าง Segment'}</small></div></div>
    <details style="margin-top:18px" ${shot.videoSegments?.length?'open':''}><summary><strong>Image-to-Video Segments (${shot.videoSegments?.length||0})</strong> · ใช้ภาพ Storyboard เป็น input_reference · Prompt ต่อคลิป 1–8 วินาที</summary>
    <div class="grid" style="margin-top:12px">${(shot.videoSegments||[]).map((seg,vi)=>`<div class="card inset segment ${seg.videoClipRelativePath?'imported':''}"><div class="spread"><span class="shot-number">SEGMENT ${vi+1} OF ${shot.videoSegments.length}</span><div><span class="badge ok">IMAGE → VIDEO</span> ${seg.safetyMode==='strict'?'<span class="badge violet">STRICT SAFETY</span>':seg.safetyRewrittenAt||seg.generationMeta?.safetyFramingApplied?'<span class="badge violet">SAFETY FRAMED</span>':''} ${seg.generationMeta?.audioRemoved?'<span class="badge ok">ไม่มีเสียง</span>':''} <span class="badge ${seg.durationSec<=8?'ok':'warn'}">${seg.durationSec}s</span> ${seg.videoStale||seg.promptStale?'<span class="badge warn">ต้องอัปเดตตาม Brief</span>':seg.videoClipRelativePath?'<span class="badge ok">✓ Video พร้อม</span>':seg.status==='blocked'?'<span class="badge warn">ถูกระบบความปลอดภัยปฏิเสธ</span>':seg.status==='generation-failed'?'<span class="badge warn">สร้างไม่สำเร็จ</span>':'<span class="badge warn">ยังไม่มี Video</span>'}</div></div><div class="segment-grid" style="margin-top:10px"><div>${field('Duration',`scenes.${si}.shots.${hi}.videoSegments.${vi}.durationSec`,seg.durationSec,{type:'number',min:1,max:8})}</div><div>${field('Image-to-video motion prompt',`scenes.${si}.shots.${hi}.videoSegments.${vi}.videoPrompt`,seg.videoPrompt,{tag:'textarea',rows:4})}</div></div>${seg.videoClipRelativePath?media(seg.videoClipRelativePath,'video',state.project.brief.aspectRatio==='9:16'):''}${seg.generationError?`<div class="inline-error"><strong>${esc(seg.generationError.title)}</strong>${esc(seg.generationError.message)}${seg.generationError.hints?.length?`<ul>${seg.generationError.hints.map(hint=>`<li>${esc(hint)}</li>`).join('')}</ul>`:''}${seg.generationMeta?.jobId?`<small>Job ID: ${esc(seg.generationMeta.jobId)}</small>`:''}</div>`:''}<div class="row wrap" style="margin-top:10px">${button(seg.videoClipRelativePath?'สร้าง Video ใหม่':'สร้าง Video ด้วย AI','generate-video','primary small',`data-si="${si}" data-hi="${hi}" data-vi="${vi}"`)}${seg.videoClipRelativePath&&!seg.generationMeta?.audioRemoved?button('ลบเสียงจาก Video','strip-video-audio','small',`data-si="${si}" data-hi="${hi}" data-vi="${vi}"`):''}${seg.status==='blocked'||seg.generationError?.code==='content_policy'?button('ใช้ Strict Safety Prompt','make-video-prompt-safe','cyan small',`data-si="${si}" data-hi="${hi}" data-vi="${vi}"`):''}${button('Copy','copy-segment','small',`data-si="${si}" data-hi="${hi}" data-vi="${vi}"`)}${button('Split','split-segment','small',`data-si="${si}" data-hi="${hi}" data-vi="${vi}"`)}${button('Duplicate','duplicate-segment','small',`data-si="${si}" data-hi="${hi}" data-vi="${vi}"`)}${button(seg.videoClipRelativePath?'Replace Import':'Import Video','import-video','small',`data-si="${si}" data-hi="${hi}" data-vi="${vi}"`)}${button('ลบ','delete-segment','danger small',`data-si="${si}" data-hi="${hi}" data-vi="${vi}"`)}</div></div>`).join('')||`<div class="empty">สร้างหรือ Import ภาพ Storyboard ก่อน แล้วกด “สร้าง Image-to-Video Segments”</div>`}</div></details></article>`;}).join('');
  return `${pageHead('Storyboard & Image-to-Video','สร้างภาพใหม่จากเรื่องราวโดยแนบ Character Sheet จริง แล้วใช้ภาพนั้นเป็น input_reference สำหรับวิดีโอ',button('สร้าง Image-to-Video ทุก Scene ที่ยังขาด','generate-all-image-to-video','primary')+button('สร้างภาพที่ขาด/ต้องอัปเดต','generate-all-storyboard-images','cyan')+button('Export Storyboard Package','export-package'))}${staleBanner('storyboard',button('อัปเดต Prompt ทุก Shot','refresh-all-shot-prompts','primary small'))}${cards?`<div class="grid">${cards}</div>`:`<div class="empty">สร้าง Story ก่อนเริ่ม Storyboard</div>`}`;
}

function voiceView() {
  const voices=state.voices||[],voiceOptions=voices.map(v=>({value:v.id,label:`${v.name} (${v.id})`})),fallbackVoice=voices[0]?.id;
  const items=dialogues().map(({scene,shot,dialogue,si,hi,di})=>{
    const character=state.project.characters.find(c=>c.id===dialogue.speakerId),capacity=(shot.videoSegments||[]).reduce((n,s)=>n+Number(s.durationSec),0)||shot.plannedDurationSec;
    const overflow=Number(dialogue.estimatedDurationSec)>capacity,profile=character?[character.gender,character.lifeStage,character.ageYears?`${character.ageYears} ปี`:'',character.personality,character.speakingStyle,character.voiceProfile?.tone].filter(Boolean).join(' · '):'Narrator profile';
    const hasCurrentAudio=Boolean(dialogue.audioRelativePath&&!dialogue.voiceStale);
    const voiceMeta=hasCurrentAudio&&dialogue.voiceIdUsed?`<small>เสียงล่าสุด: requested <code>${esc(dialogue.voiceIdUsed)}</code> → provider <code>${esc(dialogue.providerVoiceUsed||dialogue.voiceIdUsed)}</code>${dialogue.voiceFromCache?' · จาก cache':' · สร้างใหม่'}</small>`:'';
    const audio=dialogue.voiceStale&&dialogue.audioRelativePath?'<div class="prompt-box"><strong>เสียงเดิมถูกซ่อนไว้:</strong> ค่า Voice หรือการแสดงเสียงเปลี่ยนแล้ว กรุณากด “สร้างเสียงใหม่” ก่อนฟัง</div>':hasCurrentAudio?media(dialogue.audioRelativePath,'audio',false,dialogue.audioRevision||dialogue.audioGeneratedAt||''):'';
    return `<div class="card"><div class="spread"><div><span class="shot-number">S${scene.sceneNumber} / SH${shot.shotNumber}</span><h3>${esc(dialogue.speakerId==='narrator'?'Narrator':character?.name||dialogue.speakerId)}</h3></div><span class="badge ${overflow?'warn':'ok'}">${Number(dialogue.estimatedDurationSec||0).toFixed(1)}s / ${capacity}s</span></div><div class="prompt-box"><strong>Character Bible → TTS:</strong> ${esc(profile)}</div><p>“${esc(dialogue.text)}”</p><div class="grid three">${field('Voice',`voiceAssignments.${dialogue.speakerId}`,state.project.voiceAssignments[dialogue.speakerId]||character?.voiceId||fallbackVoice,{tag:'select',items:voiceOptions,help:'เลือกเสียงหลักของผู้พูด เปลี่ยนแล้วต้องกดสร้างเสียงใหม่; มะลิใช้ OpenAI coral'})}${field('Emotion',`scenes.${si}.shots.${hi}.dialogue.${di}.emotion`,dialogue.emotion||'neutral',{help:'อารมณ์ของตัวละครในประโยคนี้'})}${field('น้ำเสียง / วิธีส่งคำ',`scenes.${si}.shots.${hi}.dialogue.${di}.delivery`,dialogue.delivery||'natural',{help:'วิธีแสดง เช่น รีบร้อน กระซิบ หรือสั่นเครือ'})}${field('Speaking rate',`scenes.${si}.shots.${hi}.dialogue.${di}.pace`,dialogue.pace||1,{type:'number',min:.5,max:2,help:'1.0 = ปกติ; ต่ำกว่าจะช้าลง สูงกว่าจะเร็วขึ้น'})}</div>${voiceMeta}${audio}<div class="row wrap" style="margin-top:10px">${button(dialogue.voiceStale?'สร้างเสียงใหม่':dialogue.audioRelativePath?'Regenerate':'Generate Voice','generate-voice','primary small',`data-si="${si}" data-hi="${hi}" data-di="${di}"`)}${button('Import Audio','import-voice','small',`data-si="${si}" data-hi="${hi}" data-di="${di}"`)}${overflow?button('ย่อบท','shorten-dialogue','cyan small',`data-si="${si}" data-hi="${hi}" data-di="${di}"`):''}</div></div>`;
  }).join('');
  return `${pageHead('Voice & TTS','นำบทพูด อารมณ์ น้ำเสียง และบุคลิกจาก Character Bible มาสร้างเสียงพากย์',button('Generate ทั้งหมด','generate-all-voices','primary'))}${staleBanner('voice',button('สร้างเสียงที่ต้องอัปเดต','generate-all-voices','primary small'))}<div class="grid"><div class="card spread"><div><strong>Story + Character Bible → Voice</strong><p>OpenAI TTS จะรับ instructions จากภาษา เพศ ช่วงวัย อายุ บุคลิก วิธีพูด emotion และ delivery; <code>th-female-warm</code> จะส่งด้วยเสียง coral และบังคับการออกเสียงไทยแบบผู้หญิงอย่างชัดเจน ไฟล์เสียงเดิมต้องกด Regenerate</p></div><span class="badge ok">Thai gender-aware</span></div>${items||'<div class="empty">ยังไม่มีบทพูดใน Story</div>'}</div>`;
}

function timelineView() {
  const segs=segments(); const lines=dialogues(); const width=x=>Math.max(80,Number(x)*18);
  return `${pageHead('Timeline','เรียง Scene → Shot → Segment โดยอัตโนมัติ ตรวจไฟล์ที่ขาดและกำหนด fit mode ก่อน export',button('+ SFX','import-sfx')+button('ไป Export','go-export','primary'))}${staleBanner('timeline',button('ยืนยัน Timeline ล่าสุด','accept-timeline','small'))}
  <div class="grid four" style="margin-bottom:18px"><div class="card metric"><strong>${duration()}s</strong><span>ความยาวรวม</span></div><div class="card metric"><strong>${segs.length}</strong><span>Video segments</span></div><div class="card metric"><strong>${segs.filter(x=>x.segment.videoClipRelativePath).length}/${segs.length}</strong><span>วิดีโอพร้อม</span></div><div class="card metric"><strong>${lines.filter(x=>x.dialogue.audioRelativePath).length}/${lines.length}</strong><span>เสียงพร้อม</span></div></div>
  <div class="card timeline"><div class="track"><div class="track-label">VIDEO</div><div class="track-lane">${segs.map(x=>`<div class="clip" style="width:${width(x.segment.durationSec)}px" title="${esc(x.segment.videoClipRelativePath||'Missing video')}">S${x.scene.sceneNumber} SH${x.shot.shotNumber}<br>SEG${x.segment.segmentNumber} · ${x.segment.durationSec}s</div>`).join('')}</div></div><div class="track"><div class="track-label">DIALOGUE</div><div class="track-lane">${lines.map(x=>`<div class="clip audio" style="width:${width(x.dialogue.estimatedDurationSec)}px">${esc(x.dialogue.speakerId)}<br>${Number(x.dialogue.estimatedDurationSec).toFixed(1)}s</div>`).join('')}</div></div><div class="track"><div class="track-label">MUSIC</div><div class="track-lane">${state.project.timeline.musicRelativePath?`<div class="clip music" style="width:${width(duration())}px">Background music</div>`:'<span style="color:#6f7888;padding:13px">ยังไม่มีเพลง</span>'}</div></div><div class="track"><div class="track-label">SFX</div><div class="track-lane">${(state.project.timeline.sfx||[]).map((fx,index)=>`<div class="clip audio" style="margin-left:${Math.max(0,fx.startSec||0)*8}px">SFX ${index+1}<br>${fx.startSec||0}s <button class="icon-btn" data-action="remove-sfx" data-fi="${index}">✕</button></div>`).join('')||'<span style="color:#6f7888;padding:13px">ยังไม่มี SFX</span>'}</div></div></div>
  <div class="grid two" style="margin-top:18px"><div class="card"><h2>Timeline settings</h2>${field('Fit mode','timeline.fitMode',state.project.timeline.fitMode,{tag:'select',items:['crop','contain','blur background']})}${field('Music volume','timeline.musicVolume',state.project.timeline.musicVolume,{type:'number',min:0,max:1})}<div class="row">${button(state.project.timeline.musicRelativePath?'Replace music':'Import music','import-music')}${state.project.timeline.musicRelativePath?button('Remove','remove-music','danger'):''}</div>${state.project.timeline.musicRelativePath?media(state.project.timeline.musicRelativePath,'audio'):''}</div><div class="card"><h2>Clip trim & volume</h2>${segs.map(x=>`<div class="spread" style="border-bottom:1px solid #252b38;padding:8px 0"><span>S${x.scene.sceneNumber} SH${x.shot.shotNumber} SEG${x.segment.segmentNumber}</span><div class="row" style="width:300px"><input data-path="scenes.${x.si}.shots.${x.hi}.videoSegments.${x.vi}.trimStartSec" type="number" min="0" value="${x.segment.trimStartSec||0}" title="Trim start"><input data-path="scenes.${x.si}.shots.${x.hi}.videoSegments.${x.vi}.volume" type="number" min="0" max="2" value="${x.segment.volume??1}" title="Volume"></div></div>`).join('')||'<p>ยังไม่มี segment</p>'}</div></div>`;
}

function exportView() {
  const check=state.exportCheck||{warnings:[],errors:[]};
  return `${pageHead('Export','ตรวจความพร้อม ส่งออกชุด Story/Prompt หรือประกอบวิดีโอ H.264 + AAC ด้วย FFmpeg',button('ตรวจอีกครั้ง','refresh-checklist'))}
  <div class="grid two"><section class="card"><h2>Preflight checklist</h2><div class="checklist">${check.errors.map(x=>`<div class="check error">✕ ${esc(x)}</div>`).join('')}${check.warnings.map(x=>`<div class="check warn">⚠ ${esc(x)}</div>`).join('')}${!check.errors.length&&!check.warnings.length?'<div class="check">✓ พร้อมส่งออก</div>':''}</div></section>
  <section class="card"><h2>Export package</h2><p>สร้าง Story Markdown/JSON, Storyboard Markdown/CSV/JSON, Image/Video prompts และ Dialogue CSV ในโฟลเดอร์ exports ของโปรเจกต์</p>${button('Export package','export-package','primary')}${button('Export as ZIP','export-zip','cyan')}${button('Export Storyboard PDF','export-storyboard-pdf','cyan')}</section>
  <section class="card span-2"><div class="spread"><div><h2>Final MP4</h2><p>เรียงคลิปตาม segment และแปลงเป็น H.264 + AAC MP4</p></div><span class="badge ${check.errors.length?'warn':'ok'}">${check.errors.length?`${check.errors.length} blocking issue`:'Ready'}</span></div><div class="grid two">${field('Preset','_exportPreset',state.exportPreset||state.settings?.exportPreset||'1080x1920',{tag:'select',items:['1080x1920','1920x1080','1080x1080']})}${field('Fit mode','timeline.fitMode',state.project.timeline.fitMode,{tag:'select',items:['crop','contain']})}</div>${state.exportProgress?`<div class="progress"><i style="width:${state.exportProgress}%"></i></div><p>${state.exportProgress.toFixed(0)}%</p>`:''}<div class="row">${button(state.busy?'กำลัง Export…':'Export Final MP4','export-mp4','primary',check.errors.length||state.busy?'disabled':'')}${state.busy?button('ยกเลิก','cancel-export','danger'):''}</div></section></div>`;
}

function settingsView() {
  const s=state.settings||{}, vendors=s.aiVendors||[];
  const optionsFor=cap=>vendors.filter(v=>v.capabilities?.includes(cap==='tts'?'tts':'text')).map(v=>v.id);
  const routeMeta={story:{label:'Story',api:'POST /v1/responses'},storyboard:{label:'Storyboard Prompt',api:'POST /v1/responses'},video:{label:'Video Prompt',api:'POST /v1/responses'},tts:{label:'Text to Speech',api:'POST /v1/audio/speech'}};
  const assignments=['story','storyboard','video','tts'].map(cap=>{const meta=routeMeta[cap],providerId=s.providers?.[cap]||'mock',vendor=vendors.find(item=>item.id===providerId),endpoint=cap==='tts'&&providerId==='openai'?(s.endpoints?.openaiTts||'https://api.openai.com/v1/audio/speech'):(vendor?.endpoint||'Offline');return `<article class="card inset"><div class="spread"><div><h3 style="margin:0">${meta.label}</h3><small>${meta.api}</small></div><span class="badge ${providerId==='mock'?'warn':'ok'}">${esc(providerId)}</span></div><div class="grid two" style="margin-top:12px">${field('Vendor',`settings.providers.${cap}`,providerId,{tag:'select',items:optionsFor(cap)})}${field('Model',`settings.models.${cap}`,s.models?.[cap]||'')}${cap!=='tts'?field('Reasoning effort',`settings.reasoning.${cap}`,s.reasoning?.[cap]||(cap==='story'?'medium':'low'),{tag:'select',items:['none','low','medium','high','xhigh','max']}):''}</div><div class="prompt-box"><strong>Endpoint:</strong> ${esc(endpoint)}</div></article>`;}).join('');
  const vendorCards=vendors.filter(v=>v.id!=='mock').map((vendor,index)=>{
    const vi=vendors.indexOf(vendor), configured=s.secretsConfigured?.[vendor.id];
    return `<article class="card vendor-card"><div class="spread"><div><span class="badge ${configured?'ok':'warn'}">${configured?'Key configured':'No key'}</span><h2>${esc(vendor.name)}</h2><code>${esc(vendor.id)}</code></div>${!vendor.builtIn?button('ลบ Vendor','delete-ai-vendor','danger small',`data-provider="${attr(vendor.id)}" data-vindex="${vi}"`):''}</div>
      <div class="grid two" style="margin-top:16px">${field('ชื่อ Vendor',`settings.aiVendors.${vi}.name`,vendor.name)}${field('Protocol',`settings.aiVendors.${vi}.protocol`,vendor.protocol,{tag:'select',items:['openai-chat','openai-responses','gemini']})}<div class="span-2">${field('Text API endpoint',`settings.aiVendors.${vi}.endpoint`,vendor.endpoint,{help:'ใช้กับ Story, Storyboard Prompt และ Video Prompt; ต้องเป็น HTTPS'})}</div>${vendor.id==='openai'?`<div class="span-2">${field('OpenAI Image generation endpoint','settings.endpoints.openaiImage',s.endpoints?.openaiImage||'https://api.openai.com/v1/images/generations',{help:'ใช้สร้าง Character Sheet หรือภาพที่ไม่มีตัวละครอ้างอิง'})}</div><div class="span-2">${field('OpenAI Image edits endpoint','settings.endpoints.openaiImageEdit',s.endpoints?.openaiImageEdit||'https://api.openai.com/v1/images/edits',{help:'ใช้สร้าง Storyboard โดยแนบ Character Sheet จริงเป็น image[]'})}</div><div class="span-2">${field('OpenAI Video endpoint','settings.endpoints.openaiVideo',s.endpoints?.openaiVideo||'https://api.openai.com/v1/videos',{help:'ใช้สร้าง image-to-video โดยแนบ Storyboard เป็น input_reference'})}</div><div class="span-2">${field('OpenAI TTS endpoint','settings.endpoints.openaiTts',s.endpoints?.openaiTts||'https://api.openai.com/v1/audio/speech',{help:'ใช้เฉพาะงาน Text to Speech และใช้ API key OpenAI เดียวกัน'})}</div>`:''}<div class="span-2">${field('Test / Models endpoint',`settings.aiVendors.${vi}.modelsEndpoint`,vendor.modelsEndpoint||'',{help:'ใช้ตรวจ API key และการเชื่อมต่อ; เว้นว่างได้'})}</div>${field('Default text model',`settings.aiVendors.${vi}.defaultModel`,vendor.defaultModel||'')}${field('JSON mode',`settings.aiVendors.${vi}.supportsJsonMode`,String(Boolean(vendor.supportsJsonMode)),{tag:'select',items:['true','false']})}</div>
      <div class="field"><label>API key</label><input id="secret-${attr(vendor.id)}" type="password" autocomplete="off" placeholder="วาง key ที่นี่ — ค่าที่บันทึกแล้วจะไม่แสดงกลับ"><div class="row wrap">${button('บันทึก Key','save-secret','primary small',`data-provider="${attr(vendor.id)}"`)}${configured?button('ลบ Key','delete-secret','danger small',`data-provider="${attr(vendor.id)}"`):''}${button('Test Connection','test-provider','small',`data-provider="${attr(vendor.id)}"`)}</div></div></article>`;
  }).join('');
  const promptTemplateCards=Object.entries(s.promptTemplates||{}).map(([key,template])=>`<article class="card prompt-template-card">
    <div class="spread"><div><h2>${esc(template.label||key)}</h2><p>${esc(template.description||'')}</p></div>${button('คืนค่าเริ่มต้น','reset-prompt-template','ghost small',`data-template="${attr(key)}"`)}</div>
    <div class="row wrap template-vars"><small>ตัวแปรที่ใช้ได้:</small>${(template.variables||[]).map(name=>`<code>{{${esc(name)}}}</code>`).join('')}</div>
    <details class="variable-guide" style="margin-top:12px"><summary><strong>ตัวแปรเหล่านี้มาจากไหน?</strong></summary><div class="table-wrap"><table><thead><tr><th>ตัวแปร</th><th>Source ในโปรเจกต์</th><th>ความหมาย</th></tr></thead><tbody>${(template.variables||[]).map(name=>{const guide=template.variableGuide?.[name]||{};return `<tr><td><code>{{${esc(name)}}}</code></td><td>${esc(guide.source||'สร้างโดยระบบขณะเรียกงาน')}</td><td>${esc(guide.description||'ข้อมูลบริบทสำหรับ Template นี้')}</td></tr>`;}).join('')}</tbody></table></div><small>ตัวแปรคือข้อความ/JSON ที่ Renderer และ main process ประกอบก่อนส่งให้ AI ส่วนไฟล์ภาพจริงจะถูกแนบแยกผ่าน media API ตามที่ระบุในคำอธิบาย</small></details>
    <div class="grid two" style="margin-top:14px">${field('System prompt',`settings.promptTemplates.${key}.system`,template.system||'',{tag:'textarea',rows:14,help:'กำหนดบทบาท กฎ และลำดับความสำคัญของ AI'})}${field('User prompt',`settings.promptTemplates.${key}.user`,template.user||'',{tag:'textarea',rows:14,help:'กำหนดงาน โครงสร้าง JSON และตำแหน่งข้อมูลจากตัวแปร'})}</div>
  </article>`).join('');
  return `${pageHead('Settings','ใช้ API key เดียวได้ แต่เลือก model, endpoint และ reasoning แยกตามงาน',button('Import Settings','import-settings')+button('Export Settings','export-settings')+button('ใช้ค่าแนะนำ OpenAI','apply-openai-routing')+button('+ Custom Vendor','add-ai-vendor')+button('บันทึก Settings','save-settings','primary'))}
    ${!s.encryptionAvailable?'<div class="error-zone">OS encryption ใช้ไม่ได้ แอปจะไม่บันทึก API key แบบ plaintext</div>':''}
    <section class="card"><div class="spread"><div><h2>Workflow routing</h2><p>OpenAI key เดียวเรียกได้หลาย API โดยแอปจะเลือก route ตามงาน</p></div>${button('ใช้ค่าแนะนำ OpenAI','apply-openai-routing','small cyan')}</div><div class="grid two">${assignments}</div>${field('AI request timeout (วินาที)','settings.requestTimeoutSec',s.requestTimeoutSec||120,{type:'number',min:30,max:300,help:'แนะนำ 120–180 วินาทีสำหรับโมเดล reasoning หรือเมื่อ Brief ยาว'})}</section>
    <div class="grid two"><section class="card"><h2>การตั้งค่า OpenAI ที่แนะนำ</h2><p>Story: gpt-5.6-terra / medium<br>Storyboard Prompt: gpt-5.6-terra / low<br>Video Prompt: gpt-5.6-luna / low<br>Storyboard Image: gpt-image-2<br>Video Generation: sora-2<br>TTS: gpt-4o-mini-tts</p><div class="grid two">${field('Image model','settings.models.image',s.models?.image||'gpt-image-2')}${field('Video generation model','settings.models.videoGeneration',s.models?.videoGeneration||'sora-2')}</div><p class="prompt-box">Text <code>/v1/responses</code><br>Image ใหม่ <code>/v1/images/generations</code><br>Storyboard + Character refs <code>/v1/images/edits</code><br>Image-to-video <code>/v1/videos</code><br>TTS <code>/v1/audio/speech</code></p></section>
    <section class="card"><h2>Project & Export defaults</h2>${field('Autosave (ms)','settings.autosaveIntervalMs',s.autosaveIntervalMs||1200,{type:'number',min:500,max:30000})}${field('Default language','settings.defaultLanguage',s.defaultLanguage||'ไทย')}${field('Default platform','settings.defaultPlatform',s.defaultPlatform||'TikTok',{tag:'select',items:['TikTok','Reels','Shorts','YouTube','Custom']})}${field('Default aspect ratio','settings.defaultAspectRatio',s.defaultAspectRatio||'9:16',{tag:'select',items:['9:16','16:9','1:1']})}${field('Prompt language','settings.promptLanguage',s.promptLanguage||'English',{tag:'select',items:['English','ไทย']})}${field('FFmpeg path (เว้นว่างเพื่อใช้ bundled)','settings.ffmpegPath',s.ffmpegPath||'')}<div class="row">${button('Test FFmpeg','test-ffmpeg')}${button('เปิด Log folder','open-logs','ghost')}</div></section></div>
    <div class="spread" style="margin:30px 0 14px"><div><h2 style="margin:0">AI Vendor Registry</h2><p style="margin:5px 0;color:var(--muted)">API key ถูกเข้ารหัสใน main process และ Renderer เห็นเฉพาะสถานะ configured</p></div></div><div class="grid two">${vendorCards}</div>
    <section style="margin-top:24px"><div class="spread"><div><h2 style="margin:0">Prompt Template Registry</h2><p style="margin:6px 0;color:var(--muted)">แก้ไข System prompt และ User prompt ที่แอปนำไปใช้จริงกับ Story, Character, Storyboard Prompt และ Video Prompt</p></div>${button('คืนค่า Template ทั้งหมด','reset-all-prompt-templates','ghost')}</div><div class="grid" style="margin-top:14px">${promptTemplateCards}</div><div class="card workflow-note" style="margin-top:14px"><strong>การบันทึกและย้ายเวอร์ชัน</strong><p>กด “บันทึก Settings” เพื่อเริ่มใช้ Template ใหม่ โดย Export Settings จะรวม Prompt Templates แต่ไม่รวม API key</p></div></section>`;
}

function currentView() { if (!state.project&&!['projects','settings'].includes(state.route)) state.route='projects'; return ({projects:projectsView,brief:briefView,story:storyView,characters:charactersView,storyboard:storyboardView,voice:voiceView,timeline:timelineView,export:exportView,settings:settingsView}[state.route]||projectsView)(); }
function render() { app.innerHTML=shell(currentView()); }

function getPath(object,path) { return path.split('.').reduce((o,k)=>o?.[k],object); }
function setPath(object,path,value) { const keys=path.split('.'); const last=keys.pop(); let target=object; keys.forEach((key,index)=>{ if(target[key]===undefined) target[key]=/^\d+$/.test(keys[index+1]||last)?[]:{}; target=target[key]; }); target[last]=value; }
function normalizeOrder() { state.project.scenes.forEach((scene,si)=>{scene.sceneNumber=si+1;(scene.shots||[]).forEach((shot,hi)=>{shot.shotNumber=hi+1;(shot.videoSegments||[]).forEach((seg,vi)=>{seg.segmentNumber=vi+1;seg.timelineOrder=vi+1;});});}); }
function freshShot(number=1) { return {id:uid('shot'),shotNumber:number,plannedDurationSec:5,description:'ช็อตใหม่',purpose:'',characters:[],dialogue:[],camera:{shotSize:'Medium shot',angle:'Eye-level',movement:'Static',lens:'35mm'},action:'',environment:'',lighting:'soft cinematic light',imagePrompt:'',imageNegativePrompt:'',storyboardStyle:'',storyboardImageRelativePath:'',videoSegments:[],status:'draft',locked:false}; }

async function loadInitial() {
  state.route=routeFromHash();
  const [recent,settings,voices]=await Promise.all([window.studio.projects.list(),window.studio.settings.get(),window.studio.tts.voices()]);
  if(recent.ok)state.recents=recent.data;if(settings.ok)state.settings=settings.data;if(voices.ok)state.voices=voices.data;
  if(state.route==='export'&&state.project)await refreshChecklist(); render();
}
async function openProject(root) { const data=await invoke(window.studio.projects.open(root?{root}:{}),'กำลังเปิด…'); if(data){setProject(data);state.saveStatus='บันทึกแล้ว';toast('เปิดโปรเจกต์แล้ว');navigate('brief');} render(); }
async function refreshChecklist() { if(!state.project)return; const data=await invoke(window.studio.exports.checklist(state.project)); if(data)state.exportCheck=data; render(); }

function characterModal(index=null) {
  const c=index===null?{name:'',role:'ตัวละครหลัก',gender:'',lifeStage:'',ageYears:'',ageRange:'',appearance:'',wardrobe:'',personality:'',speakingStyle:'',voiceProfile:{},visualConsistencyPrompt:'',negativePrompt:'',referenceImages:[],voiceId:'',sheetStyle:state.project.brief.characterStyle||'Cinematic Realism'}:state.project.characters[index];
  const option=(value,label,current)=>`<option value="${attr(value)}" ${String(value)===String(current)?'selected':''}>${esc(label)}</option>`;
  modalRoot.innerHTML=`<div class="modal-backdrop"><form class="modal" id="character-form" data-index="${index??''}"><div class="spread"><h2>${index===null?'สร้าง':'แก้ไข'}ตัวละคร</h2><button type="button" class="icon-btn" data-action="close-modal">✕</button></div><div class="grid two"><div class="field"><label>ชื่อ</label><input name="name" required value="${attr(c.name)}"></div><div class="field"><label>บทบาท</label><input name="role" value="${attr(c.role)}"></div><div class="field"><label>เพศ</label><select name="gender">${['','หญิง','ชาย','ไม่ระบุ'].map(value=>option(value,value||'เลือกเพศ',c.gender)).join('')}</select></div><div class="field"><label>ช่วงวัย</label><select name="lifeStage">${['','เด็ก','วัยรุ่น','วัยทำงาน','วัยกลางคน','ผู้สูงอายุ'].map(value=>option(value,value||'เลือกช่วงวัย',c.lifeStage)).join('')}</select></div><div class="field"><label>อายุ (ปี)</label><input name="ageYears" type="number" min="0" max="120" value="${attr(c.ageYears||'')}"></div><div class="field"><label>ช่วงอายุ (คำอธิบาย)</label><input name="ageRange" value="${attr(c.ageRange)}"></div></div><div class="field"><label>บุคลิก</label><textarea name="personality">${esc(c.personality)}</textarea></div>${characterStyleField('รูปแบบ Character Sheet',c.sheetStyle||state.project.brief.characterStyle||'Cinematic Realism',{name:'sheetStyle',help:'เลือก preset หรือพิมพ์สไตล์เอง เช่น 3D Cartoon with soft vinyl texture'})}<div class="field"><label>รูปลักษณ์</label><textarea name="appearance">${esc(c.appearance)}</textarea></div><div class="field"><label>เสื้อผ้า</label><textarea name="wardrobe">${esc(c.wardrobe)}</textarea></div><div class="field"><label>วิธีพูด</label><textarea name="speakingStyle">${esc(c.speakingStyle)}</textarea></div><div class="grid two"><div class="field"><label>โทนเสียง</label><input name="voiceTone" value="${attr(c.voiceProfile?.tone||'')}"></div><div class="field"><label>จังหวะเสียง</label><input name="voicePace" value="${attr(c.voiceProfile?.pace||'')}"></div><div class="field"><label>สำเนียง</label><input name="voiceAccent" value="${attr(c.voiceProfile?.accent||'')}"></div><div class="field"><label>วัยที่เสียงฟังดู</label><input name="voiceAgeImpression" value="${attr(c.voiceProfile?.ageImpression||'')}"></div></div><div class="field"><label>Visual consistency prompt</label><textarea name="visualConsistencyPrompt">${esc(c.visualConsistencyPrompt)}</textarea></div><button class="btn primary" type="submit">บันทึกตัวละคร</button></form></div>`;
}
function clone(value){return JSON.parse(JSON.stringify(value));}

app.addEventListener('change',event=>{
  const path=event.target.dataset.path;if(!path)return;
  let root=state.project;
  if(path.startsWith('settings.')){root=state.settings;let value=event.target.type==='number'?Number(event.target.value):event.target.value;if(path.endsWith('.supportsJsonMode'))value=value==='true';setPath(root,path.slice(9),value);if(path.startsWith('settings.providers.')){const capability=path.split('.').pop(),vendor=state.settings.aiVendors.find(v=>v.id===value);if(vendor&&(!state.settings.models[capability]||state.settings.models[capability].startsWith('mock-'))){const openAiDefaults={story:'gpt-5.6-terra',storyboard:'gpt-5.6-terra',video:'gpt-5.6-luna',tts:'gpt-4o-mini-tts'};state.settings.models[capability]=value==='openai'?openAiDefaults[capability]:vendor.defaultModel;}}render();return;}
  if(path==='_exportPreset'){state.exportPreset=event.target.value;return;}
  if(!root)return;
  let value=event.target.type==='number'?Number(event.target.value):event.target.value;
  if(path.endsWith('.listenerIds'))value=String(value).split(',').map(item=>item.trim()).filter(Boolean);
  setPath(root,path,value);
  if(path.startsWith('voiceAssignments.')){
    const speakerId=path.slice('voiceAssignments.'.length);let affected=0;
    for(const item of dialogues())if(item.dialogue.speakerId===speakerId&&item.dialogue.audioRelativePath){item.dialogue.voiceStale=true;item.dialogue.pendingVoiceId=value;affected+=1;}
    if(affected){workflowSync().stale.voice=true;toast(`เปลี่ยน Voice แล้ว — ซ่อนเสียงเดิม ${affected} รายการจนกว่าจะสร้างเสียงใหม่`);}
  }
  const voiceField=path.match(/^scenes\.(\d+)\.shots\.(\d+)\.dialogue\.(\d+)\.(text|emotion|delivery|pace)$/);
  if(voiceField){
    const line=state.project.scenes[Number(voiceField[1])]?.shots?.[Number(voiceField[2])]?.dialogue?.[Number(voiceField[3])];
    if(line?.audioRelativePath){line.voiceStale=true;workflowSync().stale.voice=true;toast('ค่าการแสดงเสียงเปลี่ยนแล้ว — กรุณาสร้างเสียงใหม่');}
  }
  if(path.startsWith('brief.')||path==='title')markBriefChanged(path);
  if(path.startsWith('characters.')&&path.endsWith('.sheetStyle')){const character=state.project.characters[Number(path.split('.')[1])];if(character)character.sheetStale=true;}
  touch();if(path==='brief.characterStyle'||path.endsWith('.sheetStyle'))return;render();
});

app.addEventListener('click',async event=>{
  const nav=event.target.closest('[data-nav]');if(nav){navigate(nav.dataset.nav);return;}
  const el=event.target.closest('[data-action]');if(!el)return;const a=el.dataset.action;
  if(state.busy&&!['cancel-export','clear-error','go-settings'].includes(a))return;
  state.pendingAction=a;
  if(a==='clear-error'){state.error='';render();return;} if(a==='go-dashboard'){navigate('projects');return;} if(a==='go-settings'){navigate('settings');return;} if(a==='go-brief'){navigate('brief');return;} if(a==='go-characters'){navigate('characters');return;} if(a==='go-story'){navigate('story');return;} if(a==='go-storyboard'){navigate('storyboard');return;} if(a==='go-voice'){navigate('voice');return;} if(a==='go-export'){navigate('export');return;} if(a==='close-modal'){modalRoot.innerHTML='';return;}
  if(a==='fill-brief-example'){if((state.project.brief.concept||state.project.brief.keyMessage)&&!confirm('แทนที่ข้อมูล Brief ปัจจุบันด้วยตัวอย่าง “เสียงคุ้นหู... ผู้แปลกหน้า”?'))return;state.project.title=BRIEF_EXAMPLE.title;Object.assign(state.project.brief,{concept:BRIEF_EXAMPLE.concept,keyMessage:BRIEF_EXAMPLE.keyMessage,targetAudience:BRIEF_EXAMPLE.targetAudience,callToAction:BRIEF_EXAMPLE.callToAction,genre:BRIEF_EXAMPLE.genre,tone:BRIEF_EXAMPLE.tone,visualStyle:BRIEF_EXAMPLE.visualStyle,storyboardStyle:'Cinematic Color',constraints:BRIEF_EXAMPLE.constraints,platform:'TikTok',aspectRatio:'9:16',targetDurationSec:90,language:'ไทย'});markBriefChanged('brief.example');touch();toast('เติมข้อมูลตัวอย่างแล้ว — แก้ไขต่อได้ทันที');render();return;}
  if(a==='new-project'){const title=`คลิปใหม่ ${new Date().toLocaleDateString('th-TH')}`;const data=await invoke(window.studio.projects.create({title}),'กำลังสร้าง…');if(data){setProject(data);state.saveStatus='บันทึกแล้ว';navigate('brief');toast('สร้างโปรเจกต์แล้ว');}render();return;}
  if(a==='open-project'){await openProject();return;} if(a==='open-recent'){await openProject(el.dataset.root);return;}
  if(a==='archive-recent'){if(confirm('นำโปรเจกต์นี้ออกจากรายการล่าสุด? ไฟล์จริงจะไม่ถูกลบ')){await invoke(window.studio.projects.archive({root:el.dataset.root}));state.recents=state.recents.filter(x=>x.root!==el.dataset.root);render();}return;}
  if(a==='duplicate-project'){const data=await invoke(window.studio.projects.duplicate(),'กำลังทำสำเนา…');if(data){setProject(data);toast('สร้างสำเนาแล้ว');render();}return;}
  if(a==='save'){state.busyAction='save';state.busy='กำลังบันทึก…';render();await saveNow();state.busy='';state.busyAction='';render();return;} if(a==='undo'&&state.undo){state.undo.callback();state.undo=null;touch();render();return;}
  if(a==='generate-story'){const readiness=characterReadiness();if(!readiness.ready){state.error=friendlyError({code:'character_required',message:readiness.characters.length?`ยังขาด Character Sheet หรือรูปอ้างอิงของ: ${readiness.missingReference.map(character=>character.name).join(', ')}`:'ยังไม่มีตัวละครใน Character Bible'});navigate('characters');render();return;}const data=await invoke(window.studio.ai.generateStory(state.project),'AI กำลังสร้าง Story จาก Brief และ Character Bible ล่าสุด…');if(data){const lockedScenes=(Array.isArray(state.project.scenes)?state.project.scenes:[]).filter(scene=>scene?.locked);const newScenes=Array.isArray(data.scenes)?data.scenes:[];if(!data.story||!newScenes.length){state.error=friendlyError({code:'malformed_response',message:'AI ส่ง Story กลับมาไม่ครบ กรุณาลองใหม่หรือเปลี่ยนโมเดล'});render();return;}state.project.story=data.story;state.project.scenes=[...lockedScenes,...newScenes];clearWorkflowStale('story');normalizeOrder();touch();toast('อัปเดต Story ตาม Brief และ Character Bible ล่าสุดแล้ว');navigate('story');}render();return;}
  if(a==='add-scene'){state.project.scenes.push({id:uid('scene'),sceneNumber:state.project.scenes.length+1,title:'ฉากใหม่',purpose:'',location:'',timeOfDay:'day',settingDescription:'',atmosphere:'',mood:'',tone:'',narrativeBeat:'',emotionalArc:'',locked:false,shots:[freshShot()]});touch();render();return;}
  const si=Number(el.dataset.si),hi=Number(el.dataset.hi),vi=Number(el.dataset.vi),di=Number(el.dataset.di),ci=Number(el.dataset.ci);const scene=state.project?.scenes?.[si],shot=scene?.shots?.[hi],seg=shot?.videoSegments?.[vi],line=shot?.dialogue?.[di];
  if(a==='toggle-shot-character'){const character=state.project.characters[ci];shot.characters||=[];const index=shot.characters.indexOf(character.id);if(index>=0)shot.characters.splice(index,1);else shot.characters.push(character.id);shot.imagePrompt='';shot.storyboardImageRelativePath='';shot.status='draft';touch();render();return;}
  if(a==='lock-scene'){scene.locked=!scene.locked;touch();render();return;}if(a==='lock-shot'){shot.locked=!shot.locked;touch();render();return;}
  if(a==='move-scene-up'||a==='move-scene-down'){const to=si+(a.endsWith('up')?-1:1);if(to>=0&&to<state.project.scenes.length){[state.project.scenes[si],state.project.scenes[to]]=[state.project.scenes[to],state.project.scenes[si]];normalizeOrder();touch();render();}return;}
  if(a==='delete-scene'){const removed=state.project.scenes.splice(si,1)[0];rememberUndo(`Scene ${removed.sceneNumber}`,()=>{state.project.scenes.splice(si,0,removed);normalizeOrder();});normalizeOrder();touch();render();return;}
  if(a==='add-shot'){scene.shots.push(freshShot(scene.shots.length+1));touch();render();return;}
  if(a==='move-shot-up'||a==='move-shot-down'){const to=hi+(a.endsWith('up')?-1:1);if(to>=0&&to<scene.shots.length){[scene.shots[hi],scene.shots[to]]=[scene.shots[to],scene.shots[hi]];normalizeOrder();touch();render();}return;}
  if(a==='delete-shot'){const removed=scene.shots.splice(hi,1)[0];rememberUndo(`Shot ${removed.shotNumber}`,()=>{scene.shots.splice(hi,0,removed);normalizeOrder();});normalizeOrder();touch();render();return;}
  if(a==='add-dialogue'){shot.dialogue.push({id:uid('line'),speakerId:'narrator',listenerIds:[],text:'',emotion:'neutral',delivery:'natural',pace:1,startSec:0,estimatedDurationSec:3,audioRelativePath:''});touch();render();return;}
  if(a==='prompt-shot'){if(shot.imagePrompt&&!confirm('Shot นี้มี Image Prompt แล้ว ต้องการสร้างใหม่และแทนที่ Prompt เดิมหรือไม่?'))return;const data=await createShotPrompt(scene,shot);if(data){toast('สร้าง Image Prompt แล้ว');if(state.route==='story'&&!shot.storyboardImageRelativePath&&state.settings?.secretsConfigured?.openai&&confirm(`สร้าง Prompt แล้ว ต้องการสร้างภาพ Storyboard แบบ ${shot.storyboardStyle||state.project.brief.storyboardStyle||'Cinematic Color'} ต่อทันทีหรือไม่? การสร้างภาพอาจมีค่าใช้จ่าย`))await createStoryboardImage(scene,shot);}render();return;}
  if(a==='generate-characters'){const data=await invoke(window.studio.ai.generateCharacters(state.project),'AI กำลังทบทวนตัวละครจาก Brief ล่าสุด…');if(data){clearWorkflowStale('characters');if(data.length){state.project.characters.push(...data);touch();toast(`เพิ่มตัวละคร ${data.length} คนแล้ว — สร้าง Character Sheet ให้ครบก่อน Story`);}else toast('AI ตรวจแล้ว ไม่พบตัวละครใหม่ — ใช้ Character Bible เดิมต่อได้');}render();return;}
  if(a==='accept-characters'){clearWorkflowStale('characters');touch();toast('ยืนยันใช้ Character Bible เดิมกับ Brief ล่าสุดแล้ว');render();return;}
  if(a==='apply-character-style-all'){const style=state.project.brief.characterStyle||'Cinematic Realism';for(const character of state.project.characters){character.sheetStyle=style;character.sheetStale=true;}workflowSync().stale.characters=true;touch();toast(`ตั้ง ${style} ให้ทุกตัวแล้ว — กดสร้าง Character Sheet ใหม่`);render();return;}
  if(a==='add-character'){characterModal();return;}if(a==='edit-character'){characterModal(ci);return;}
  if(a==='generate-character-sheet'){const character=state.project.characters[ci],hasSheet=character.referenceImages?.some(image=>image.source==='ai-character-sheet');if(hasSheet&&!confirm(`ตัวละคร “${character.name}” มี Character Sheet แล้ว ต้องการสร้างใหม่หรือไม่?`))return;if(!confirm(`สร้างภาพ ${character.sheetStyle||'Cinematic Realism'} แบบ 4 มุมด้วย ${state.settings?.models?.image||'gpt-image-2'}? การทำงานนี้อาจมีค่าใช้จ่าย`))return;const data=await createCharacterSheet(character);if(data)toast(`สร้าง Character Sheet ของ ${character.name} แล้ว`);render();return;}
  if(a==='generate-all-character-sheets'){const missing=(state.project.characters||[]).filter(character=>character.sheetStale||!character.referenceImages?.some(image=>image.source==='ai-character-sheet'));if(!missing.length){toast('ตัวละครทุกตัวมี Character Sheet ล่าสุดแล้ว');return;}if(!confirm(`จะสร้าง Character Sheet ${missing.length} ภาพตาม Style ล่าสุด ซึ่งอาจมีค่าใช้จ่ายตาม OpenAI API ดำเนินการต่อหรือไม่?`))return;let completed=0;for(const character of missing){const data=await createCharacterSheet(character);if(!data)break;completed++;}if(completed)toast(`สร้าง Character Sheet แล้ว ${completed}/${missing.length} ตัวละคร`);render();return;}
  if(a==='duplicate-character'){const copy=clone(state.project.characters[ci]);copy.id=uid('character');copy.name+=' (สำเนา)';copy.referenceImages=[];state.project.characters.splice(ci+1,0,copy);touch();render();return;}
  if(a==='delete-character'){const removed=state.project.characters.splice(ci,1)[0];rememberUndo(removed.name,()=>state.project.characters.splice(ci,0,removed));touch();render();return;}
  if(a==='import-character-image'){const data=await invoke(window.studio.assets.import({kind:'character'}));if(data){const character=state.project.characters[ci],refs=character.referenceImages||=[];refs.push({id:uid('image'),relativePath:data.relativePath,caption:data.name,isPrimary:!refs.length});character.sheetStale=false;if(state.project.characters.every(item=>!item.sheetStale&&item.referenceImages?.length))clearWorkflowStale('characters');touch();render();}return;}
  if(a==='primary-character-image'){state.project.characters[ci].referenceImages.forEach((x,i)=>x.isPrimary=i===Number(el.dataset.ii));touch();render();return;}
  if(a==='generate-storyboard-image'){if(shot.storyboardImageRelativePath&&!confirm('Shot นี้มีภาพ Storyboard แล้ว ต้องการสร้างภาพ AI ใหม่และเปลี่ยนภาพที่ใช้งานหรือไม่?'))return;const data=await createStoryboardImage(scene,shot);if(data)toast(`สร้างภาพ Storyboard แบบ ${data.style} แล้ว`);render();return;}
  if(a==='generate-all-storyboard-images'){const missing=shots().filter(item=>item.shot.storyboardStale||!item.shot.storyboardImageRelativePath);if(!missing.length){toast('ทุก Shot มีภาพ Storyboard ล่าสุดแล้ว');return;}if(!confirm(`จะสร้างภาพ Storyboard AI ${missing.length} ภาพตาม Brief ล่าสุด ซึ่งอาจมีค่าใช้จ่ายตาม OpenAI API ดำเนินการต่อหรือไม่?`))return;let completed=0;for(const item of missing){const data=await createStoryboardImage(item.scene,item.shot);if(!data)break;completed++;}if(completed)toast(`สร้างภาพ Storyboard แล้ว ${completed}/${missing.length} ภาพ`);render();return;}
  if(a==='import-storyboard'){const data=await invoke(window.studio.assets.import({kind:'storyboardImage'}));if(data){shot.storyboardImageRelativePath=data.relativePath;shot.storyboardStale=false;shot.videoPromptStale=Boolean(shot.videoSegments?.length);(shot.videoSegments||[]).forEach(segment=>{segment.promptStale=true;if(segment.videoClipRelativePath)segment.videoStale=true;});shot.status='image-imported';if(shots().every(item=>!item.shot.promptStale&&!item.shot.storyboardStale))clearWorkflowStale('storyboard');touch();render();}return;}
  if(a==='remove-storyboard'){if(confirm('นำภาพนี้ออกจากโปรเจกต์? Video Segment เดิมจะถูกทำเครื่องหมายว่าต้องอัปเดต')){await invoke(window.studio.assets.remove({relativePath:shot.storyboardImageRelativePath}));shot.storyboardImageRelativePath='';shot.videoPromptStale=Boolean(shot.videoSegments?.length);(shot.videoSegments||[]).forEach(segment=>{segment.promptStale=true;if(segment.videoClipRelativePath)segment.videoStale=true;});touch();render();}return;}
  if(a==='copy-shot-prompt'){await navigator.clipboard.writeText(`${shot.imagePrompt}\n\nNegative: ${shot.imageNegativePrompt}`);toast('คัดลอก Prompt แล้ว');return;}
  if(a==='refresh-all-shot-prompts'){const items=shots();if(!items.length){toast('ยังไม่มี Shot ให้อัปเดต');return;}if(!confirm(`สร้าง Storyboard Prompt ใหม่ ${items.length} Shot ตาม Brief ล่าสุด? Prompt เดิมจะถูกแทนที่ แต่ภาพและวิดีโอเดิมจะยังเก็บไว้พร้อมป้าย stale`))return;let completed=0;for(const item of items){const data=await createShotPrompt(item.scene,item.shot);if(!data)break;completed++;}if(completed)toast(`อัปเดต Prompt แล้ว ${completed}/${items.length} Shot`);render();return;}
  if(a==='generate-segments'){if(!shot.storyboardImageRelativePath){state.error=friendlyError({code:'storyboard_required',message:'Shot นี้ยังไม่มีภาพ Storyboard สำหรับใช้เป็น input_reference'});render();return;}if(shot.videoSegments?.length&&!confirm(`Shot นี้มี ${shot.videoSegments.length} Image-to-Video Segments แล้ว การสร้างใหม่จะแทนที่ Prompt เดิม ดำเนินการต่อหรือไม่?`))return;if(shot.plannedDurationSec>8&&!confirm(`ช็อตยาว ${shot.plannedDurationSec} วินาที ระบบจะแบ่งเป็น ${Math.ceil(shot.plannedDurationSec/8)} segments (แต่ละช่วงไม่เกิน 8 วินาที) ดำเนินการต่อ?`))return;const data=await createVideoSegments(scene,shot);if(data&&state.settings?.secretsConfigured?.openai&&confirm(`สร้าง Image-to-Video Segments แล้ว ต้องการสร้างวิดีโอจริง ${data.length} คลิปด้วย Sora ต่อทันทีหรือไม่? ทุกคลิปจะใช้ภาพ Storyboard เป็น input_reference`)){let completed=0;for(const segment of shot.videoSegments){const video=await createSegmentVideo(scene,shot,segment,false);if(!video)break;completed++;}toast(`สร้างวิดีโอแล้ว ${completed}/${data.length} คลิป`);}render();return;}
  if(a==='generate-all-image-to-video'){
    const all=shots(),blocked=all.filter(item=>!item.shot.storyboardImageRelativePath&&(!item.shot.videoSegments?.length||item.shot.videoSegments.some(segment=>!segment.videoClipRelativePath||segment.videoStale||segment.promptStale))),eligible=all.filter(item=>item.shot.storyboardImageRelativePath&&(!item.shot.videoSegments?.length||item.shot.videoPromptStale||item.shot.videoSegments.some(segment=>!segment.videoClipRelativePath||segment.videoStale||segment.promptStale)));
    if(!eligible.length){toast(blocked.length?`ยังสร้างไม่ได้: ${blocked.length} Shot ไม่มีภาพ Storyboard`:'ทุก Scene/Shot มีวิดีโอล่าสุดครบแล้ว');return;}
    const estimatedClips=eligible.reduce((total,item)=>total+((!item.shot.videoSegments?.length||item.shot.videoPromptStale||item.shot.videoSegments.some(segment=>segment.promptStale))?Math.ceil(Math.max(1,Number(item.shot.plannedDurationSec)||1)/8):item.shot.videoSegments.filter(segment=>!segment.videoClipRelativePath||segment.videoStale).length),0);
    if(!confirm(`จะสร้าง Image-to-Video สำหรับ ${eligible.length} Shot ที่ยังขาด (ประมาณ ${estimatedClips} คลิป) ด้วย ${state.settings?.models?.videoGeneration||'sora-2'} ซึ่งอาจมีค่าใช้จ่ายและใช้เวลานาน${blocked.length?`\n\nข้าม ${blocked.length} Shot ที่ยังไม่มีภาพ Storyboard`:''}\n\nดำเนินการต่อหรือไม่?`))return;
    let prepared=0,created=0,policyBlocked=0,failed=false;
    for(const [shotIndex,item] of eligible.entries()){
      const mustRegenerate=!item.shot.videoSegments?.length||item.shot.videoPromptStale||item.shot.videoSegments.some(segment=>segment.promptStale);
      if(mustRegenerate){const data=await createVideoSegments(item.scene,item.shot,`กำลังเตรียม Shot ${shotIndex+1}/${eligible.length}`);if(!data){failed=true;break;}prepared++;}
      const missing=item.shot.videoSegments.filter(segment=>!segment.videoClipRelativePath||segment.videoStale);
      for(const segment of missing){const video=await createSegmentVideo(item.scene,item.shot,segment,false,`Shot ${shotIndex+1}/${eligible.length} · Video ${created+policyBlocked+1}/${estimatedClips}`);if(!video){if(segment.status==='blocked'){policyBlocked++;state.error='';continue;}failed=true;break;}created++;}
      if(failed)break;
    }
    if(!failed&&shots().every(item=>!item.shot.videoPromptStale&&(item.shot.videoSegments||[]).every(segment=>segment.videoClipRelativePath&&!segment.promptStale&&!segment.videoStale)))clearWorkflowStale('video');
    touch();toast(`ประมวลผลแล้ว: เตรียม Segment ${prepared} Shot · สร้าง Video ${created}/${estimatedClips} คลิป${policyBlocked?` · moderation ${policyBlocked} คลิป (สร้างคลิปอื่นต่อแล้ว)`:''}${blocked.length?` · ข้าม ${blocked.length} Shot ที่ไม่มี Storyboard`:''}${failed?' · หยุดเมื่อพบข้อผิดพลาด':''}`);render();return;
  }
  if(a==='make-video-prompt-safe'){const data=await invoke(window.studio.ai.makeVideoPromptSafe({project:state.project,shot,videoPrompt:seg.videoPrompt,strict:true}),'กำลังสร้าง Strict Safety Prompt ใหม่…');if(data?.videoPrompt){seg.videoPrompt=data.videoPrompt;seg.safetyRewrittenAt=data.rewrittenAt;seg.safetyMode=data.safetyMode;seg.status='prompt-ready';seg.promptStale=false;seg.videoStale=Boolean(seg.videoClipRelativePath);seg.generationError=null;state.error='';touch();toast('สร้าง Strict Safety Prompt ใหม่แล้ว — ตรวจข้อความและลองสร้าง Video อีกครั้ง');}render();return;}
  if(a==='strip-video-audio'){const data=await invoke(window.studio.assets.stripVideoAudio({relativePath:seg.videoClipRelativePath}),'กำลังลบเสียงจาก Video โดยไม่ลดคุณภาพภาพ…');if(data?.audioRemoved){seg.generationMeta={...(seg.generationMeta||{}),audioRemoved:true};touch();toast('ลบเสียงจาก Video แล้ว');}render();return;}
  if(a==='copy-segment'){await navigator.clipboard.writeText(`${seg.videoPrompt}\n\nNegative: ${seg.videoNegativePrompt}`);toast('คัดลอก Video Prompt แล้ว');return;}
  if(a==='split-segment'){if(seg.durationSec<=1){toast('Segment สั้นเกินไปที่จะแบ่ง');return;}const first=Math.ceil(seg.durationSec/2),second=seg.durationSec-first;seg.durationSec=first;const copy=clone(seg);copy.id=uid('segment');copy.durationSec=second;copy.videoClipRelativePath='';copy.startFrame=`Continue from segment ${vi+1}`;shot.videoSegments.splice(vi+1,0,copy);normalizeOrder();touch();render();return;}
  if(a==='duplicate-segment'){const copy=clone(seg);copy.id=uid('segment');copy.videoClipRelativePath='';shot.videoSegments.splice(vi+1,0,copy);normalizeOrder();touch();render();return;}
  if(a==='delete-segment'){const removed=shot.videoSegments.splice(vi,1)[0];rememberUndo(`Segment ${vi+1}`,()=>{shot.videoSegments.splice(vi,0,removed);normalizeOrder();});normalizeOrder();touch();render();return;}
  if(a==='generate-video'){if(seg.promptStale){toast('Brief เปลี่ยนแล้ว — กรุณากด “สร้าง Segments ใหม่” ของ Shot นี้ก่อนสร้างวิดีโอ');return;}if(seg.videoClipRelativePath&&!confirm('Segment นี้มีวิดีโอแล้ว ต้องการสร้างใหม่และเปลี่ยนคลิปที่ใช้งานหรือไม่?'))return;if(!confirm(`สร้างวิดีโอ ${seg.durationSec} วินาทีด้วย ${state.settings?.models?.videoGeneration||'sora-2'}? การทำงานนี้อาจมีค่าใช้จ่ายและใช้เวลาหลายนาที`))return;const data=await createSegmentVideo(scene,shot,seg);if(data)toast('สร้างและดาวน์โหลดวิดีโอแล้ว');render();return;}
  if(a==='import-video'){const data=await invoke(window.studio.assets.import({kind:'video'}));if(data){seg.videoClipRelativePath=data.relativePath;seg.videoStale=false;seg.status='video-imported';if(segments().every(item=>!item.segment.promptStale&&!item.segment.videoStale))clearWorkflowStale('video');touch();render();}return;}
  if(a==='generate-voice'){await generateVoice(si,hi,di);render();return;}if(a==='generate-all-voices'){for(const x of dialogues())if(!x.dialogue.audioRelativePath||x.dialogue.voiceStale)await generateVoice(x.si,x.hi,x.di,false);clearWorkflowStale('voice');toast('สร้างเสียงตาม Story ล่าสุดครบแล้ว');render();return;}
  if(a==='import-voice'){const data=await invoke(window.studio.assets.import({kind:'voice'}));if(data){line.audioRelativePath=data.relativePath;line.voiceStale=false;line.audioRevision=Date.now();line.audioGeneratedAt=new Date().toISOString();line.voiceIdUsed='imported';line.providerVoiceUsed='imported audio';line.voiceFromCache=false;if(dialogues().every(item=>!item.dialogue.voiceStale))clearWorkflowStale('voice');touch();render();}return;}
  if(a==='shorten-dialogue'){const data=await invoke(window.studio.ai.shortenDialogue({text:line.text,maxWords:Math.max(5,Math.floor(shot.plannedDurationSec*2.2))}));if(data){line.text=data.text;line.estimatedDurationSec=Math.max(1,line.text.split(/\s+/).length/2.2);touch();render();}return;}
  if(a==='import-music'){const data=await invoke(window.studio.assets.import({kind:'music'}));if(data){state.project.timeline.musicRelativePath=data.relativePath;touch();render();}return;}if(a==='remove-music'){state.project.timeline.musicRelativePath='';touch();render();return;}
  if(a==='import-sfx'){const data=await invoke(window.studio.assets.import({kind:'sfx'}));if(data){state.project.timeline.sfx.push({id:uid('sfx'),relativePath:data.relativePath,startSec:0,volume:1,fadeInSec:0,fadeOutSec:0});touch();render();}return;}if(a==='remove-sfx'){state.project.timeline.sfx.splice(Number(el.dataset.fi),1);touch();render();return;}
  if(a==='refresh-checklist'){await refreshChecklist();return;}if(a==='export-package'){await saveNow();const data=await invoke(window.studio.exports.package(state.project),'กำลังสร้าง Package…');if(data){toast(`Export แล้ว: ${data.target}`);}render();return;}
  if(a==='export-zip'){await saveNow();const data=await invoke(window.studio.exports.zip(state.project),'กำลังบีบอัดเป็น ZIP…');if(data){toast(`Export ZIP แล้ว: ${data.outputPath}`);}render();return;}
  if(a==='export-storyboard-pdf'){await saveNow();const data=await invoke(window.studio.exports.storyboardPdf(state.project),'กำลังสร้าง Storyboard PDF…');if(data){toast(`Export PDF แล้ว: ${data.outputPath} (${data.pageCount} หน้า)`);}render();return;}
  if(a==='export-external-character-prompt'){await saveNow();const data=await invoke(window.studio.exports.characterPrompt({project:state.project,promptTemplates:state.settings.promptTemplates}),'กำลัง Render System Prompt และ User Prompt สำหรับสร้างตัวละคร…');if(data)toast(`Export Character System + User Prompt แล้ว: ${data.target}`);render();return;}
  if(a==='export-external-storyboard-prompt'){await saveNow();const data=await invoke(window.studio.exports.storyboardPrompt({project:state.project,promptTemplates:state.settings.promptTemplates}),'กำลังจัด Story System/User, Full Story Prompt, Shot Prompt และ Character References…');if(data)toast(`Export System/User + Full Story + ${data.shotCount} Shot พร้อมภาพอ้างอิง ${data.referenceCount} ไฟล์แล้ว: ${data.target}`);render();return;}
  if(a==='accept-timeline'){clearWorkflowStale('timeline');touch();toast('ยืนยัน Timeline ตามข้อมูลล่าสุดแล้ว');render();return;}
  if(a==='export-mp4'){state.exportProgress=1;state.busyAction='export-mp4';state.busy='กำลังประกอบวิดีโอ…';render();const result=await window.studio.exports.mp4({project:state.project,options:{preset:state.exportPreset||state.settings.exportPreset,fitMode:state.project.timeline.fitMode}});state.busy='';state.busyAction='';if(result.ok&&result.data){toast(`Export MP4 แล้ว: ${result.data.outputPath}`);}else if(!result.ok)state.error=friendlyError(result.error);state.exportProgress=0;render();return;}
  if(a==='cancel-export'){await window.studio.exports.cancel();state.busy='';state.busyAction='';state.exportProgress=0;render();return;}
  if(a==='apply-openai-routing'){Object.assign(state.settings.providers,{story:'openai',storyboard:'openai',video:'openai',tts:'openai'});Object.assign(state.settings.models,{story:'gpt-5.6-terra',storyboard:'gpt-5.6-terra',video:'gpt-5.6-luna',tts:'gpt-4o-mini-tts',image:'gpt-image-2',videoGeneration:'sora-2'});state.settings.reasoning={story:'medium',storyboard:'low',video:'low'};state.settings.endpoints={...(state.settings.endpoints||{}),openaiTts:'https://api.openai.com/v1/audio/speech',openaiImage:'https://api.openai.com/v1/images/generations',openaiImageEdit:'https://api.openai.com/v1/images/edits',openaiVideo:'https://api.openai.com/v1/videos'};const openai=state.settings.aiVendors.find(vendor=>vendor.id==='openai');if(openai){openai.protocol='openai-responses';openai.endpoint='https://api.openai.com/v1/responses';openai.modelsEndpoint='https://api.openai.com/v1/models';openai.defaultModel='gpt-5.6-terra';openai.supportsJsonMode=true;}toast('ตั้งค่า OpenAI แยกตามงานแล้ว — กดบันทึก Settings');render();return;}
  if(a==='reset-prompt-template'){const key=el.dataset.template,defaults=state.settings.promptTemplateDefaults?.[key];if(defaults&&confirm(`คืนค่า System และ User prompt ของ ${defaults.label}?`)){state.settings.promptTemplates[key]=clone(defaults);toast(`คืนค่า ${defaults.label} แล้ว — กดบันทึก Settings`);render();}return;}
  if(a==='reset-all-prompt-templates'){if(confirm('คืนค่า Prompt Templates ทั้งหมดเป็นค่าเริ่มต้น?')){state.settings.promptTemplates=clone(state.settings.promptTemplateDefaults||{});toast('คืนค่า Prompt Templates ทั้งหมดแล้ว — กดบันทึก Settings');render();}return;}
  if(a==='export-settings'){const saved=await invoke(window.studio.settings.save(state.settings),'กำลังตรวจสอบ Settings…');if(!saved){render();return;}state.settings=saved;const data=await invoke(window.studio.settings.export(),'กำลัง Export Settings…');if(data)toast(`Export Settings แล้ว (ไม่รวม API key): ${data.path}`);render();return;}
  if(a==='import-settings'){if(!confirm('Import จะเขียนทับ routing, models, endpoints และค่าเริ่มต้นปัจจุบัน แต่จะไม่เปลี่ยน API key ในเครื่องนี้ ดำเนินการต่อ?'))return;const data=await invoke(window.studio.settings.import(),'กำลัง Import Settings…');if(data){state.settings=data.settings;toast('Import Settings แล้ว — API key ต้องตั้งแยกในเครื่องนี้');}render();return;}
  if(a==='add-ai-vendor'){const id=`custom_${Date.now().toString(36)}`;state.settings.aiVendors.push({id,name:'Custom AI Vendor',protocol:'openai-chat',endpoint:'',modelsEndpoint:'',defaultModel:'',capabilities:['text'],builtIn:false,supportsJsonMode:false});render();setTimeout(()=>document.querySelector(`[data-path="settings.aiVendors.${state.settings.aiVendors.length-1}.name"]`)?.focus(),0);return;}
  if(a==='delete-ai-vendor'){const provider=el.dataset.provider,index=Number(el.dataset.vindex);if(!confirm(`ลบ ${provider} ออกจาก Vendor Registry?`))return;await window.studio.settings.deleteSecret({provider});state.settings.aiVendors.splice(index,1);for(const capability of Object.keys(state.settings.providers))if(state.settings.providers[capability]===provider)state.settings.providers[capability]='mock';state.settings.secretsConfigured[provider]=false;const data=await invoke(window.studio.settings.save(state.settings));if(data)state.settings=data;render();return;}
  if(a==='save-settings'){const data=await invoke(window.studio.settings.save(state.settings));if(data){state.settings=data;toast('บันทึก Settings แล้ว');render();}return;}
  if(a==='save-secret'){const provider=el.dataset.provider,key=document.querySelector(`#secret-${provider}`).value;if(!key){toast('กรอก API key ก่อน');return;}const saved=await invoke(window.studio.settings.save(state.settings));if(!saved){render();return;}state.settings=saved;const data=await invoke(window.studio.settings.saveSecret({provider,secret:key}));if(data){state.settings.secretsConfigured[provider]=true;toast('บันทึก key แบบเข้ารหัสแล้ว');render();}return;}
  if(a==='delete-secret'){await invoke(window.studio.settings.deleteSecret({provider:el.dataset.provider}));state.settings.secretsConfigured[el.dataset.provider]=false;render();return;}
  if(a==='test-provider'){const saved=await invoke(window.studio.settings.save(state.settings));if(!saved){render();return;}state.settings=saved;const data=await invoke(window.studio.settings.testProvider({provider:el.dataset.provider}),'กำลังทดสอบการเชื่อมต่อ…');if(data)toast(data.message);render();return;}
  if(a==='test-ffmpeg'){const data=await invoke(window.studio.settings.testFfmpeg());if(data)toast(data.message);render();return;}if(a==='open-logs'){await window.studio.settings.openLogs();return;}
});

modalRoot.addEventListener('submit',event=>{if(event.target.id!=='character-form')return;event.preventDefault();const fd=new FormData(event.target),index=event.target.dataset.index;const current=index===''?{id:uid('character'),referenceImages:[],voiceId:''}:state.project.characters[Number(index)];for(const key of ['name','role','gender','lifeStage','ageRange','appearance','wardrobe','personality','speakingStyle','visualConsistencyPrompt','sheetStyle'])current[key]=fd.get(key);current.ageYears=Math.max(0,Math.min(120,Number(fd.get('ageYears'))||0));current.voiceProfile={genderPresentation:current.gender,ageImpression:String(fd.get('voiceAgeImpression')||current.lifeStage||''),tone:String(fd.get('voiceTone')||''),pace:String(fd.get('voicePace')||''),accent:String(fd.get('voiceAccent')||'')};current.negativePrompt||='identity drift, wardrobe change';if(index==='')state.project.characters.push(current);touch();modalRoot.innerHTML='';render();});
modalRoot.addEventListener('click',event=>{const el=event.target.closest('[data-action="close-modal"]');if(el)modalRoot.innerHTML='';});

async function generateVoice(si,hi,di,notify=true){
  const line=state.project.scenes[si].shots[hi].dialogue[di];
  const character=state.project.characters.find(item=>item.id===line.speakerId);
  const voiceId=state.project.voiceAssignments[line.speakerId]||character?.voiceId||state.voices?.[0]?.id;
  const forceRegenerate=Boolean(line.audioRelativePath||line.voiceStale);
  const characterProfile=character?{id:character.id,name:character.name,gender:character.gender,lifeStage:character.lifeStage,ageYears:character.ageYears,personality:character.personality,speakingStyle:character.speakingStyle,voiceProfile:character.voiceProfile}:null;
  const data=await invoke(window.studio.tts.synthesize({dialogueId:line.id,text:line.text,voiceId,language:state.project.brief.language==='ไทย'?'th-TH':'en-US',rate:Number(line.pace)||1,emotion:line.emotion,delivery:line.delivery,characterProfile,forceRegenerate}),'กำลังสร้างเสียงจากบทพูดและ Character Bible…');
  if(data){
    line.audioRelativePath=data.relativePath;line.estimatedDurationSec=data.durationSec;line.voiceStale=false;
    line.audioRevision=Date.now();line.audioGeneratedAt=new Date().toISOString();line.voiceIdUsed=data.requestedVoice||voiceId;line.providerVoiceUsed=data.providerVoice||voiceId;line.voiceFromCache=Boolean(data.cached);delete line.pendingVoiceId;
    if(dialogues().every(item=>!item.dialogue.voiceStale))clearWorkflowStale('voice');touch();
    if(notify)toast(forceRegenerate?`สร้างเสียงใหม่แล้ว: ${line.voiceIdUsed} → ${line.providerVoiceUsed}`:data.cached?'ใช้ไฟล์เสียงจาก cache':`สร้างเสียงแล้ว: ${line.voiceIdUsed} → ${line.providerVoiceUsed}`);
  }
}

async function createShotPrompt(scene,shot){const hadImage=Boolean(shot.storyboardImageRelativePath);const data=await invoke(window.studio.ai.generateShotPrompt({project:state.project,scene,shot}),'AI กำลังสร้าง Storyboard Prompt…');if(data){Object.assign(shot,data);shot.promptStale=false;if(hadImage)shot.storyboardStale=true;shot.status='prompt-ready';touch();}return data;}
async function createVideoSegments(scene,shot,batchLabel=''){const data=await invoke(window.studio.ai.generateVideoSegments({project:state.project,scene,shot}),`${batchLabel?`${batchLabel} · `:''}กำลังสร้าง Image-to-Video action beats จากภาพ Storyboard…`);if(data){shot.videoSegments=data;shot.videoPromptStale=false;if(shots().every(item=>!item.shot.videoPromptStale))clearWorkflowStale('video');normalizeOrder();touch();render();}return data;}
async function createCharacterSheet(character){const data=await invoke(window.studio.ai.generateCharacterSheet({project:state.project,character}),'AI กำลังสร้าง Character Sheet หลายมุม…');if(data){const refs=character.referenceImages||=[];refs.forEach(image=>image.isPrimary=false);refs.push({id:uid('image'),relativePath:data.relativePath,caption:`${data.style} turnaround sheet`,isPrimary:true,source:'ai-character-sheet',style:data.style,generatedAt:data.generatedAt,model:data.model});character.sheetStyle=data.style;character.sheetStale=false;if(state.project.characters.every(item=>!item.sheetStale&&item.referenceImages?.length))clearWorkflowStale('characters');touch();}return data;}
async function createStoryboardImage(scene,shot){if(!shot.imagePrompt||shot.promptStale){const prompt=await createShotPrompt(scene,shot);if(!prompt)return null;}const style=shot.storyboardStyle||state.project.brief.storyboardStyle||'Cinematic Color';const data=await invoke(window.studio.ai.generateStoryboardImage({project:state.project,scene,shot:{...shot,storyboardStyle:style}}),`AI กำลังวาดภาพ Storyboard แบบ ${style} โดยอ้างอิง Character Sheet…`);if(data){const hadSegments=Boolean(shot.videoSegments?.length);shot.storyboardStyle=data.style;shot.storyboardImageRelativePath=data.relativePath;shot.promptStale=false;shot.storyboardStale=false;shot.imageGenerationMeta={provider:'openai',model:data.model,generatedAt:data.generatedAt,style:data.style,generationMode:data.generationMode,referenceCount:data.referenceCount||0,referenceCharacterIds:data.referenceCharacterIds||[]};shot.status='image-generated';if(hadSegments){shot.videoPromptStale=true;(shot.videoSegments||[]).forEach(segment=>{segment.promptStale=true;if(segment.videoClipRelativePath)segment.videoStale=true;});}if(shots().every(item=>!item.shot.promptStale&&!item.shot.storyboardStale))clearWorkflowStale('storyboard');touch();}return data;}
async function createSegmentVideo(scene,shot,segment,notify=true,batchLabel=''){state.mediaProgress=1;segment.generationError=null;segment.status='generating';const data=await invoke(window.studio.ai.generateVideo({project:state.project,scene,shot,segment}),`${batchLabel?`${batchLabel} · `:''}กำลังสร้าง Image-to-Video Segment ${segment.segmentNumber} จากภาพ Storyboard…`);state.mediaProgress=0;if(data){segment.videoClipRelativePath=data.relativePath;segment.promptStale=false;segment.videoStale=false;segment.status='video-generated';segment.generationError=null;segment.generationMeta={...(segment.generationMeta||{}),provider:'openai',model:data.model,jobId:data.jobId,generatedAt:data.generatedAt,generatedSeconds:data.generatedSeconds,generationMode:data.generationMode,audioRemoved:Boolean(data.audioRemoved),safetyFramingApplied:Boolean(data.safetyFramingApplied),inputReferenceRelativePath:data.inputReferenceRelativePath};if(segments().every(item=>item.segment.videoClipRelativePath&&!item.segment.promptStale&&!item.segment.videoStale))clearWorkflowStale('video');touch();if(notify)toast('สร้าง Image-to-Video Segment แล้ว (ไม่มีเสียงจากคลิป)');}else{const error=typeof state.error==='string'?friendlyError({message:state.error}):state.error||friendlyError();segment.status=error.code==='content_policy'?'blocked':'generation-failed';segment.generationError={title:error.title,message:error.message,hints:error.hints,code:error.code};segment.generationMeta={...(segment.generationMeta||{}),safetyFramingApplied:Boolean(error.details?.safetyFramingApplied)};if(error.details?.jobId)segment.generationMeta.jobId=error.details.jobId;touch();}return data;}

window.addEventListener('hashchange',async()=>{state.route=routeFromHash();if(state.route==='export'&&state.project)await refreshChecklist();render();});
window.addEventListener('studio:render',render);window.addEventListener('studio:status',()=>{document.querySelector('#save-status')?.replaceChildren(document.createTextNode(`● ${state.saveStatus}`));document.querySelector('#bottom-status')?.replaceChildren(document.createTextNode(state.saveStatus));});
window.studio.onExportProgress(data=>{state.exportProgress=data.progress;const bar=document.querySelector('.progress i');if(bar)bar.style.width=`${data.progress}%`;});
window.studio.onMediaProgress(data=>{state.mediaProgress=Math.min(100,Math.max(0,Number(data.progress)||0));if(data.segmentId&&data.jobId){const item=segments().find(entry=>entry.segment.id===data.segmentId);if(item)item.segment.generationMeta={...(item.segment.generationMeta||{}),jobId:data.jobId};}const label=document.querySelector('.activity-bar strong'),bar=document.querySelector('.activity-track i');if(label)label.textContent=`กำลังสร้างวิดีโอ: ${data.status||'processing'} · ${state.mediaProgress.toFixed(0)}%`;if(bar){bar.style.animation='none';bar.style.width=`${state.mediaProgress}%`;}});

loadInitial();
